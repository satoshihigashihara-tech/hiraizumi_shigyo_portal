# ひらいずみ志業ポータル 作業一覧

確認日：2026年9月12日／基準ブランチ：`main`

## 0. 現在の正本：MVP完成・公開・発表までのロードマップ

この章が2026年9月12日時点の進行判断の正本です。第2章以降には実装途中の時点で書いた検証記録も残しているため、「ローカル作成中」「未適用」「画面なし」などの過去形の記述は、ここおよび各節末尾の新しい実績記録より優先しません。

認証、キャンプ申請、地域活動個人申請、団体代表者申請、代表者の招待発行、参加者の招待確認・入力・提出、公開カレンダー、キャンプ・地域活動個人の職員審査画面はmainへ反映済みです。DBのマイグレーションはSQL001〜029がmainにあります。未完了作業はGitHub Issueで管理し、完了したIssueは対応PRのマージと同時に閉じます。

### A1 対象者ID・管理用氏名・参加状態・既存データ移行基盤（SQL 030・ローカル実装・検証済み）

A1はスパルタキャンプだけを対象に、既存 `camp_eligible_users.id` を安定IDとして使うDB・取得・診断基盤を追加する。SQL 001〜029は変更しない。既存campは `legacy_application`、既存申請の `camp_eligible_user_id` はNULLのままで、一斉リンクとモード切替を行わない。

SQL 030は対象者の管理用氏名、資格と独立した `participating / released`、終了根拠、Auth削除後も残る本人結合、applicationsとの複合FK、対象者単位の有効申請UQ、camp限定整合性トリガー、camp・入力の版列を追加する。新方式の最初の下書き作成時だけ確認済みAuthメールを使って結合し、以後は対象者IDと申請所有者を正とする。メール変更や同じメールの別Authで所有者を移さない。

職員向け読取RPCと非更新の移行診断RPCを追加する。診断はsnapshot一意一致、空snapshot draftの現在確認済みメール、snapshot/current競合、一致なし、複数有効申請、無効対象者と開いた申請、滞在開始・完了、期間・部屋・stay・定員不整合を分類する。候補を返してもリンク、補正、campモード変更はしない。

A1では対象者登録RPC/UI、事前部屋割り、PDF、フォーム、許可後部屋変更、room_allocations・calendar_claims・stays、料金・納付・取消・延泊、地域活動個人・団体を変更しない。重複登録時に `disabled_at` をNULLへ戻す既存動作はA2で扱う。

ローカルPostgreSQL 18.4でSQL 001〜030の適用、単体1,602チェック、既存機能を含む並行91ケースと全テスト後の隔離スキーマ削除を確認した。`npm test` 364件、ESLint、本番ビルドも成功している。本番ビルドは `next/font` のGoogle Fonts取得を含むため、ネットワーク許可付きで確認した。Supabase適用、push、PR、マージは未実施。

### A2 新方式対象者の単件登録・編集（SQL 031・ローカル検証済み）

A2は`eligible_roster`だけに、管理用氏名とメールアドレスの単件登録・期待版付き編集を追加する。同campのメール重複は有効・無効を問わず拒否し、既存対象者を更新・復活しない。結合済み対象者のメール変更は理由を必須にし、Auth結合、申請、profiles、地域活動、legacy campを変更しない。ローカルPostgreSQL 18.4でSQL001〜031を適用し、A2単体15項目、A2別接続競合1件、既存単体・競合回帰を確認した。Node 373件、ESLint、webpack本番build、差分検査も成功した。SQL031のSupabase本番適用、commit、push、PR、マージ、実職員の画面受入は未実施である。

| 優先 | Issue | 残作業 | 完了の目安 |
|---|---:|---|---|
| 完了 | [#87](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/87) | 団体参加者の個人情報入力・確認・提出 | PR #91でmainへ反映。SQL028はSupabaseへ適用済み |
| 完了 | [#77](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/77) | 代表者の参加者削除・交代・取消 | PR #92でmainへ反映。実データで参加者削除と再招待可能状態を確認済み |
| P04 | [#80](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/80) | 職員の団体審査・部屋割り・許可・取消 | ローカル画面接続済み。実職員で主要状態遷移を受入確認する |
| P05 | [#78](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/78) | 職員カレンダー・利用停止期間 | 実職員のChrome受入確認済み。PRをmainへ反映してIssueを完了する |
| P06 | [#86](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/86) | キャンプ編集・削除・対象者変更 | 実装・SQL029適用・DBテスト・実職員の画面確認済み。PR作成待ち |
| P07 | [#81](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/81) | 個人申請の取消・継続申請 | 状態・期間制約を守って実データで完了できる |
| P08 | [#84](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/84) | アカウント停止・初期化案内 | 画面接続済み。実アカウントを使う停止後の拒否と再利用手順を受入確認する |
| P09 | [#82](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/82) | 保護者同意書の実Storage受入 | 本人・職員だけが実ファイルを扱える |
| P10 | [#89](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/89) | 権限・期限・二重操作などの異常系 | 部分更新や秘密情報の露出なく拒否できる |
| P11 | [#83](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/83) | 団体申請E2E受入 | 代表者・参加者・職員の一連操作が完了する |
| P12 | [#85](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/85) | Vercel本番公開 | 本番URLで認証と主要正常系が動く |
| P13 | [#88](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/88) | 発表スライド・デモ・通信障害時の代替資料 | 通信断でも30秒以内に代替へ移れる |
| P14 | [#79](https://github.com/satoshihigashihara-tech/hiraizumi_shigyo_portal/issues/79) | 最終総合確認・発表練習 | 重大不具合なく時間内に発表できる |

進行順はP02からP14です。前倒しできる独立作業はあっても、公開前にP10・P11、発表前にP12・P13を完了させます。公開障害やデモ失敗へ対応できる余裕を残し、残トークンを理由に実装品質、確認、説明を省略しません。

## 1. 最初に読むこと

現在は主要なバックエンドと一部の利用者・職員画面がmainへ反映され、残る画面接続、実Storage受入、異常系、団体E2E、本番公開、発表準備を進める段階です。完成状態ではありませんが、Next.js初期テンプレートだけの段階ではありません。

この一覧は、[要件定義](requirements.md)、[画面・URL設計](routes.md)、[データベース設計](database.md)、現在のソース、マージ済みPRを照合したものです。詳細なSQL適用・DBテスト・ブラウザー受入の履歴は後半に残します。Vercel公開と全役割の総合E2Eは未完了です。

| 表記 | 意味 |
|---|---|
| 実装済み | 記載した範囲のコード・SQLが存在する。実環境での動作確認済みという意味ではない |
| 作業中 | 一部の処理・テーブルはあるが、要件を満たすには残作業がある。誰かが現在着手していることを断定しない |
| 未着手 | 対応する実装をリポジトリ内で確認できない。設計書に書かれているだけでは実装済みにしない |
| フロント担当 | 画面、入力フォーム、表示、画面遷移を作る人 |
| バックエンド担当 | 保存処理、DB、認証・権限、料金・定員などの業務ルールを作る人 |
| 共同 | 2人で接続・動作確認・仕様確認を行う |

担当は2人開発を想定した**仮の役割分担**です。実名・着手日・期限は資料にないため未確定です。以下の「完了条件」は今後の確認基準であり、達成済みの報告ではありません。

用語：DBはデータの保存先、SQLマイグレーションはDB構造を順番に作るファイル、Server Actionは画面から呼ぶサーバーの更新処理、RPCはDB内の処理を呼ぶ仕組み、RLSはDB側で「誰がどのデータを見られるか」を制限する仕組みです。

## 2. 実装済みの部品と確認根拠

ここは部品単位の一覧です。部品を使う機能全体の残作業は第3章で管理します。

| ID | 状態 | 部品・確認できた内容 | 根拠ファイル | 担当 | 完了条件（部品の確認） | フロント担当への引き渡し |
|---|---|---|---|---|---|---|
| P01 | 実装済み | Supabaseのブラウザ・サーバー接続とセッション更新 | `utils/supabase/client.js`、`server.js`、`middleware.js`、`proxy.js` | バックエンド | ログイン情報をサーバーで取得・更新できる | 接続部品は利用候補。`proxy.js` はセッション更新だけで、画面のアクセス制限は別途必要 |
| P02 | 実装済み | ログイン・新規登録・ログアウトActionと接続画面 | `app/actions/auth.js`、`app/login/`、`app/signup/` | 共同 | 架空アカウントで各操作と役割別遷移を確認する | `login`、`signUp`、`logout`を画面へ接続済み。実アカウント確認はT02・T24で行う |
| P03 | 実装済み | プロフィール・職員権限・8部屋／合計15人・デモ連絡先のSQL | `supabase/migrations/202609100001_initial_setup.sql` | バックエンド | DB適用後に登録時のプロフィール作成、本人制限、部屋定員を確認する | プロフィール項目と部屋一覧の土台あり。部屋割り処理は未完成 |
| P04 | 実装済み | キャンプ用の申請・採番・料金・部屋割り・滞在・状態履歴テーブルとRLS | `supabase/migrations/202609100002_camp_applications.sql` | バックエンド | DB適用後、本人・職員だけが申請関連情報を読める | 保存先は存在。審査・納付・入退去の更新操作まで実装済みとは扱わない |
| P05 | 実装済み | キャンプ下書き作成・保存・提出ActionとRPC、料金計算・採番・提出時の15人制限 | `app/actions/camp-applications.js`、`supabase/migrations/202609100003_camp_application_actions.sql` | バックエンド | 下書きでは採番せず、提出の再送で同じ受付結果を返し、料金・状態履歴が一致する | フォーム接続候補あり。ただしT04・T05の検証と補完が必要 |
| P06 | 実装済み | 職員のキャンプ新規作成・対象メール一括追加ActionとRPC | `app/actions/staff-camps.js`、`supabase/migrations/202609100004_staff_camp_actions.sql` | バックエンド | 職員以外を拒否し、別キャンプとの期間重複・メール形式を検査する | 新規登録・追加の土台。SQL 012による編集・削除とエラー返却変更はP11・第4.6節を参照 |
| P07 | 実装済み | 同意書保存・差し替え・60秒の閲覧URL発行、PDF/JPEG/PNG・5MiB検査、提出時の同意書メタデータ確認 | `app/actions/guardian-consent.js`、SQL `005`〜`007`、`next.config.mjs` | バックエンド | 実ファイルの保存・閲覧・差し替え・不正提出拒否を確認する | 添付UI接続候補。非公開バケット設定とT06の確認が引き渡し条件 |
| P08 | 実装済み | 有効な利用者・職員を確認する共通ガード、保護layout、本人プロフィール保存Action | `utils/auth/guards.js`、`app/user/layout.js`、`app/staff/layout.js`、`app/actions/profile.js` | 共同 | 未ログイン・停止中・職員以外を拒否し、本人のプロフィールだけを保存できる | layoutへ共通ガードを接続済み。プロフィールフォームを `saveProfile` へ接続する |
| P09 | 実装済み | 職員による明示的な審査開始・修正依頼・不許可ActionとRPC。SQL 010で更新日時照合・監査履歴・不許可時の割当解放を追加 | `app/actions/staff-applications.js`、SQL `008`・`010` | バックエンド | 状態遷移、理由、修正期限、職員権限、履歴、古い画面の拒否をDB・画面から確認する | 現ActionはSQL 010適用が必要。既存3つのActionにも `updatedAt` を追加する |
| P10 | 実装済み | キャンプ個人の部屋割り・許可ActionとRPC、日別定員検査、職員専用監査履歴、許可時の入居前登録。SQL適用済み・単一接続218項目と同時更新7ケース成功 | `app/actions/staff-applications.js`、SQL `010` | バックエンド | 権限・状態・定員・履歴・同時更新をDBで確認し、職員画面へ接続する | `assignCampApplicationRoom`・`approveCampApplication`。第4.4・4.5節のDB検証済み。画面接続と受入確認は残作業 |
| P11 | 実装済み | キャンプ・利用停止の枠、公開／職員カレンダー取得、キャンプ編集・削除、停止期間管理。SQL 012ローカル検証済み・Supabase適用成功はユーザー確認済み | SQL `012`、`staff-calendar.js`、`staff-camps.js`、`utils/calendar/` | バックエンド | Supabase上の業務RPC、画面・実ログインで確認する | 第4.6節とルート設計9.4節。個人の追加はP12、団体はT18以降 |
| P12 | 実装済み | 地域活動個人の下書き・提出・修正再提出・本人取得、個人枠、料金・採番・監査、職員の最小審査。SQL 013適用とDB検証・後片付けが完了 | SQL `013`、`community-applications.js`、`staff-community-applications.js`、`utils/community-applications/` | バックエンド | ローカル・SupabaseのDB検証は完了。画面接続後に実ログイン・同意書の実ファイル操作を確認する | 第4.7節とルート設計9.5節のAction・入力・返却契約を利用可能。画面・保護layoutは未作成 |

SQL 014（地域活動個人の部屋割当・許可）は作業ブランチに実装・ローカル検証済みで、Supabase適用と既存回帰を含む単一接続916項目の成功をユーザーが確認しました。別接続60ケースと全後片付けの成功もユーザー確認済みです。詳細は第4.8節。

SQLは `001` から `013` の順で確認します。Supabase適用済みは `013` までです。009・010・011・012・013の適用成功はユーザー報告に基づきます。013適用後のSupabase検証は第4.7節に記録します。`006` が提出処理を変更し、`007` がロック処理と同意書登録権限を修正し、`008` が職員の審査操作を追加し、`009` が再提出期限を修正し、`010` が部屋割り・許可と審査RPCの更新日時照合を追加し、`011` が下書き作成入口の期限判定時刻を修正し、`012` がキャンプ・利用停止の枠とカレンダー取得・更新を、`013` が地域活動個人の申請・枠・料金・採番・最小審査を追加します。古いSQLだけで現状を判断しないでください。

SQL 009は初回提出と修正再提出の期限を分け、ロック取得後の現在時刻で期限を判定します。修正期限未設定・期限切れ・キャンプ日程変更時の再提出を拒否します。SQL 011は下書き作成入口の時刻を `now()` から `clock_timestamp()` へ変更します。両方とも既存Actionの名前・入力項目は維持しています。

検証結果：2026年9月10日、SQL 001〜011適用後にユーザーが `supabase/tests/camp_room_allocations_and_approval.sql` 全体をSQL Editorで再実行しました。15:42の提供画像で `passed_checks = 218` と単一接続の表示を確認しました。初回に失敗した下書き作成入口の期限拒否は、SQL 011で修正後、追加ケースとともに通過しました。確認範囲は第4.4節に記載します。

検証SQLは架空データと一時ヘルパーを使い、`authenticated`／`anon` へ権限を切り替えてRPC・RLSを検査します。履歴書込みの失敗を起こす検証用トリガーも含め、末尾の `ROLLBACK` で検証用変更を取り消します。Authの実ログイン、JWT署名検証、Storageの実ファイル検証、複数接続での同時更新はこの218項目には含みません。当時はローカルのPostgreSQL実行環境・直接DB接続は未設定でした。今回のSQL 012検証用に一時的なローカルDBを用意しました（第4.6節）。

コードの確認：`npm run lint`・`npm run build`・`git diff --check` は成功しています。SQL 009・011は元の関数との差分を静的に確認しました。職員Actionの一時的な検証では34項目が成功しました（認証ガード、入力拒否、更新日時のマイクロ秒保持、RPC引数、エラー変換、表示更新等）。この34項目はSupabaseと認証ガードを代替しており、DBの検証結果とは区別します。

## 3. 機能ごとの作業一覧

### 3.1 最初に進める：基盤・キャンプの申請

| ID | 状態 | 作業・現在地 | 担当 | 完了条件 | フロント担当への引き渡し |
|---|---|---|---|---|---|
| T01 | 作業中 | SQL 001〜013、環境変数、非公開Storage、架空の職員・対象者アカウント、テスト用キャンプは設定済み（009・010・011・012・013適用成功はユーザー報告）。画面経由の疎通と公開環境は未確認 | バックエンド | SQLの関数を実際に呼び、架空アカウントで認証・権限・保存・添付を確認する。Vercelにも同じ環境変数を安全に設定する | 起動手順、デモ用ログイン手順、利用可能な処理と未確認事項を渡す。秘密鍵は画面・文書・Gitに含めない |
| T02 | 作業中 | `/login`・`/signup`・`/forbidden`、利用者・職員layoutの認証保護を実装。未ログインのURL直接入力と安全な戻り先をローカルブラウザで確認済み | 共同（処理：バックエンド／画面：フロント） | 架空の一般利用者・職員・停止中アカウントで実ログインし、役割別遷移と案内を確認する。各データ取得とActionの認可も維持する | P02・P08を接続済み。登録確認メールと実アカウントによる受入確認はT24で行う |
| T03 | 作業中 | `/user/profile`で本人の氏名・住所・電話・緊急連絡先を実データから取得・保存する画面を接続。入力失敗時の保持、成功・取得失敗表示あり | 共同 | 実アカウントで表示・保存を確認し、次回のキャンプ申請へ初期表示されることを確認する。提出済みの写しは変更しない | RLSと本人IDで取得・更新。既存`saveProfile`の互換性を維持し、画面は`saveProfileState`へ接続 |
| T04 | 実装・受入済み | キャンプ設定と対象者管理。編集・削除・対象メールの変更／無効化まで画面へ接続。SQL029適用・DBテスト9件・実職員での画面表示成功 | 共同 | PRを作成し、レビュー後にマージする | `/staff/camps` 配下。対象者変更は理由・版照合・監査を必須にする |
| T05 | 作業中 | キャンプ申請。入口・対象キャンプ選択・`/edit`・`/confirm`・`/complete`・本人詳細を実データへ接続。プロフィール初期値、固定期間、月別料金、下書き保存、確認同意、提出、受付結果、申請・納付・滞在の状態、公開履歴、現在の部屋を実装 | 共同 | 対象キャンプ選択→入力→保存→確認→提出→完了→詳細を操作できる。期限、確認同意、二重送信、修正再提出を確認する | P05、第4章のフォーム項目・遷移先を使用。完了情報はDBから取得し、提出＝利用確定と表示しない |
| T06 | 作業中 | 同意書添付。Action・メタデータSQLに加え、本人の`/edit`へ添付済み表示・閲覧・添付・差し替えUIを接続。実Storageの画面受入確認と直接RPC対策が残る | 共同（保存と権限：バックエンド） | 申請ごとに1ファイルを保存・差し替えでき、本人と職員だけ閲覧できる。空ファイル、形式偽装、5MiB超過、実ファイルなしの提出を拒否する | 実アカウントでPDF・JPEG・PNG、空ファイル、形式偽装、5MiB超過、差し替え、他人の閲覧拒否を確認する |
| T07 | 作業中 | 公開トップに利用案内・条件・料金・必要書類・ログイン導線、公開カレンダー、地域活動個人申請への入口あり | フロント | 公開カレンダーと地域活動個人申請を実環境で確認し、団体申請入口を実データへ接続する | `/calendar`はT09の公開取得契約だけを使用し、個人情報・内部理由を表示しない |
| T08 | 作業中 | `/user/applications`と`/user`を本人の実データへ接続し、申請・納付・滞在、料金、部屋、受付番号、次に必要な操作を表示。キャンプ・地域活動個人・団体参加者の詳細へ接続 | フロント（取得処理はバックエンドと共同） | 実アカウントで一覧・ホーム・3利用区分の詳細を確認する | RLSと本人IDの両方で絞り、固定許可リストだけを返す。団体参加者へ他参加者の個人情報を渡さない |

根拠：要件5〜9・20〜21章、ルート5〜8・13章、DB5.1〜5.3・5.6・9章。

### 3.2 次に進める：日程・地域活動・職員の業務

| ID | 状態 | 作業・現在地 | 担当 | 完了条件 | フロント担当への引き渡し |
|---|---|---|---|---|---|
| T09 | 画面接続済み・受入待ち | SQL012の職員月・日取得と利用停止管理を`/staff/calendar`、`/staff/calendar/blocked-periods`配下へ接続。キャンプ、個人、団体、利用停止を区別し、内部理由は職員だけに表示する | 共同 | 実職員で作成、競合拒否、編集、古い版拒否、削除確認を実データで確認する。一般利用者へ内部情報を出さない | 閲覧では状態を変更しない。削除は理由と明示確認を画面・Server Action双方で必須にする |
| T10 | 作業中 | SQL 013のバックエンドとDB検証に加え、地域活動個人の新規・入力・確認・完了・詳細を実データへ接続。日程、目的、町内活動、料金見込み、同意書、状態履歴、部屋・滞在を表示 | 共同 | 実アカウントで下書き作成・保存・同意書添付・提出・完了・詳細まで確認する。開始14日前・終了60日以内・2〜15日の拒否も画面で確認する | 共通URLは本人所有の利用区分を先に確認し、キャンプ・地域活動個人・団体参加者を振り分ける |
| T11 | 作業中 | キャンプ審査・許可・古い画面の拒否・不許可時の割当解放をSQL 010とActionへ追加。単一接続218項目と同時更新7ケース成功。画面は未確認。SQL 013に地域活動個人の審査開始・修正依頼・不許可を追加（Supabase適用済み）。SQL 014に個人の許可を追加（ローカル検証済み・Supabase適用・DB検証・後片付け完了）。キャンセル確定は未実装 | バックエンド→フロント | 閲覧だけで審査を開始しない。理由必須の操作、状態ごとの編集制限、許可条件、枠解放、変更履歴、古い更新日時での保存拒否を一括処理する | P09・P10、第4.4節を職員詳細へ接続。キャンプ利用者には本人用キャンセルボタンを出さない |
| T12 | 作業中 | キャンプ個人はSQL 010で実装・検証済み。地域活動個人の部屋割当・許可をSQL 014とActionへ追加し、既存回帰を含むローカル単一接続916項目・別接続60ケース・Node 112テスト成功。SQL 014のSupabase適用と既存回帰を含む単一接続916項目はユーザー確認済み。Supabaseの別接続60ケースと全後片付けもユーザー確認済み。画面接続は未実施。団体は未実装（第4.8節） | バックエンド→フロント | 各部屋定員と施設15人を日別に検査する。個人は割当完了後のみ許可、団体は参加人数と割当人数の一致も必要。許可後変更は理由と前後内容を残す | P10、第4.4節。キャンプ個人は部屋だけ選択し人数はDBで1人固定。申請者には割当結果を表示する |
| T13 | 作業中 | 料金・納付。キャンプ提出時の計算とテーブルあり。SQL 013で地域活動個人の提出・日程変更再提出の計算を追加（ローカル検証済み、Supabase適用成功をユーザー確認済み）。納付更新と画面なし | バックエンド→フロント | 600円・9,600円・9,000円、同月別申請6,000円＋3,300円を確認する。職員が納付期限・未納／納付済みを更新し、未納の期限超過を表示する。他の状態を自動変更しない | 個別詳細へ月別内訳・合計・納付状態、職員詳細へ更新操作を渡す。団体代表者には見込合計と納付済み人数のみ |
| T14 | 作業中 | 滞在・設定テーブルあり。SQL 010にキャンプ許可時、SQL 014に地域活動個人許可時の入居前登録を追加。SQL016の入退去・早期退去はmain・Supabaseへ適用済みで、実DB242項目成功をユーザー確認済み。画面なし | バックエンド→フロント | 許可時に入居前、対面確認で滞在中→退去済みとする。早期退去では元期間・料金を維持し、個人は翌日、団体は全員退去の翌日から枠解放、キャンプ枠は維持する | 職員詳細に入退去操作、滞在中の利用者と許可後の団体代表者に連絡先を表示。設定画面は `/staff/settings` |
| T15 | 作業中 | 個別状態履歴あり。SQL 010・013・014の既存監査に加え、SQL017でキャンプ利用者監査と個別申請staff_notesを実装。main・Supabaseへ適用済みで、実DB323項目成功をユーザー確認済み。画面なし | バックエンド→フロント | 全更新に操作者・日時・対象・前後内容を記録する。本人用の状態履歴と職員専用メモ・監査履歴を分離する | 第4.4節。内部の部屋変更理由は `audit_logs` だけから職員向けに取得する |
| T16 | 作業中 | SQL018の検索RPCとServer取得を`/staff`へ接続。まずキャンプ申請の氏名・受付番号・キャンプ名・期間、申請／納付／滞在状態、50件ページング、正規詳細URLに対応。実職員で0件表示と404を確認。地域活動個人・団体の画面と実データ検索は未確認 | 共同 | `/staff` で氏名・団体名・期間検索、利用区分・申請／納付／滞在状態の絞り込みを行い、正しい詳細へ進める | キャンプ検索と詳細はIssue #55。地域活動個人・団体は各画面接続後に検索対象へ追加する。本人画面へ職員検索結果を渡さない |
| T17 | 画面接続済み・受入待ち | SQL019・020はmain／Supabase反映済み。本人の取消・継続申請画面と詳細・一覧の導線を接続 | 共同 | 取消は理由付きで申請し職員確定まで枠を保持する。継続は元申請を変更せず別申請を作る。実データで入居後・重複継続の拒否を確認する | `/cancel`・`/extension`を既存契約へ接続済み。`original_application_id`がある行は「継続申請」と表示して元申請へリンクする |

根拠：要件6・10〜11・13〜19・21章、ルート6.2・7章、DB5・6〜9章。

### 3.3 続いて進める：団体・自動処理・仕上げ

| ID | 状態 | 作業・現在地 | 担当 | 完了条件 | フロント担当への引き渡し |
|---|---|---|---|---|---|
| T18 | 画面接続済み・受入継続 | SQL021と代表者向け団体一覧・新規・編集・確認・完了・詳細をmainへ反映。実団体作成と公開カレンダーの専有枠を確認済み | 共同 | 残る団体E2Eで代表者非宿泊を含む全状態を確認する | Issue #83で総合確認する |
| T19 | 受入準備中 | SQL022・023、代表者の参加者一覧・招待、参加者の招待確認・個別入力・確認・提出・完了・詳細をmainへ反映。SQL028もSupabase適用済み | 共同 | 実アカウントで初回提出・差し戻し再提出・全員提出時の団体審査中遷移を確認する | 認証前は団体情報を見せず、他参加者の個人情報を取得しない |
| T20 | 画面接続済み・受入待ち | SQL024・025はmain／Supabase反映済み。代表者の変更・取消画面はmain反映・実データ確認済み。職員の団体一覧・審査・部屋割り・許可・取消画面はローカル接続済み | 共同 | 実職員で目的確認、参加者審査、人数一致の部屋割り、団体許可・不許可・取消を受入確認する。期限切れ自動処理はT21 | `/staff/community/groups`と`/staff/community/groups/[groupId]`を既存SQL024・025へ接続済み。新しいSQLはない |
| T21 | バックエンド適用済み・受入継続 | SQL026をmain・Supabaseへ反映し、期限切れ処理、Cron登録、提出との競合、保存しない回帰SQLの成功を確認済み | 共同 | Issue #89・#83で画面表示と実時間境界を含む異常系・団体E2Eを確認する | 締切・期限切れ表示と問い合わせ案内を渡す。通常のページ閲覧で更新を実行しない。個人申請に同じ自動取消を適用しない |
| T22 | 画面接続済み・受入待ち | 利用終了後のアカウント初期化・職員による停止 | 共同 | SQL027とEdge Functionを適用し、実DB20項目成功。申請詳細の停止操作と停止中の案内を接続。Vault未設定のため自動削除は無効 | 架空の停止対象で実行し、保護画面拒否、内部理由非表示、初期化後の再登録案内を確認する。ジョブ・秘密値は画面へ出さない |
| T23 | 完了 | 利用者画面の375px／PC幅、ラベル、Tab操作とフォーカス、文字付き状態、入力保持、エラーリンク、送信中、空表示を確認。共通`loading.js`・`error.js`・`not-found.js`を追加。Chromeで公開4画面を375px実測し、52〜233ms・横はみ出しなし。認証後画面は共通CSSと自動テストで確認。Safariでトップと404、Edgeでトップを表示確認 | フロント（確認は共同） | スマホ／PCで主要操作ができ、ラベル・キーボード操作・文字による状態説明・入力保持・エラー位置への移動・送信中表示を備える。主要画面おおむね3秒を目標に確認する | Chrome・Safari・Edgeの確認結果をIssue #25のPRへ記録。実データを使う認証後の操作時間はT24の受入確認でも再確認する |
| T24 | 受入準備中 | 総合受入確認・デモデータ・README・公開確認 | 共同 | README、総合確認表、5役割デモ手順、緊急対応を作成。画面完成後に実ログイン・Storage・Vercelの確認結果を記録する | `docs/mvp-acceptance.md`を共同で更新し、未確認項目を完成扱いにしない |
| T25 | 未着手 | 本運用前の町への確認 | 共同（町への確認窓口は未定） | 要件28章の制度・料金・電子申請の効力・保存期間・同意書様式等の決定と影響を記録する | 決定後に文言・入力項目・保存条件へ反映。試作版の開発は現行要件で進められる。定員15人は確認済み |

根拠：要件7.3・12・23・25〜28章、ルート6.3・7.3・14章、DB5.4・9〜10・12〜13章。

## 4. フロント担当が接続を始めるためのメモ

### 4.1 既存Actionの入力と動き

以下は現在のコードの契約です。設計との差分は第5章で修正対象として管理します。Action名が存在しても、遷移先ページは未作成です。

| ファイル／Action | 渡す値（フォームの `name`） | 現在の成功時の動き |
|---|---|---|
| `app/actions/auth.js`：`login` / `signUp` | `email`、`password`、任意の `returnTo` | 利用者は `/user`、職員ログインは `/staff`、条件を満たす招待経由は招待先へ戻る |
| 同上：`logout` | 引数なし | `/login` へ移動する（設計では `/`） |
| `app/actions/profile.js`：`saveProfile` | `fullName`、`address`、`phone`、`emergencyName`、`emergencyAddress`、`emergencyPhone` | `/user/profile?saved=1`。全項目は空欄保存も可能 |
| `app/actions/staff-applications.js`：`startCampApplicationReview` | `applicationId`、`updatedAt` | 申請済みから審査中へ変更し、職員の申請詳細へ戻る。SQL 010必須 |
| 同上：`requestCampApplicationRevision` / `rejectCampApplication` | `applicationId`、`updatedAt`、`reason` | 審査中から修正依頼または不許可へ変更し、職員の申請詳細へ戻る。SQL 010必須 |
| 同上：`assignCampApplicationRoom` | `applicationId`、`updatedAt`、`roomId`、`reason`（割当済みの部屋変更では必須） | 審査中または許可後の部屋を保存し、職員詳細へ `?updated=room-assigned` で戻る。SQL 010必須 |
| 同上：`approveCampApplication` | `applicationId`、`updatedAt`、任意の `approvalComment` | 審査中から許可へ変更し、入居前を登録。職員詳細へ `?updated=approved` で戻る。SQL 010必須 |
| `app/actions/staff-camps.js`：`createStaffCamp` | `campName`、`startDate`、`endDate`、`applicationDeadline` | `/staff/camps/[campId]` へ移動。期限入力は `YYYY-MM-DDTHH:mm` を日本時間として変換 |
| 同上：`addCampEligibleUsers` | `campId`、`eligibleEmails` | 対象者画面へ登録件数などをクエリで返す。メールは改行・空白・カンマ・セミコロン区切りに対応 |
| `app/actions/camp-applications.js`：`createCampApplicationDraft` | `campId` | `/user/applications/[applicationId]/edit` へ移動 |
| 同上：`saveCampApplicationDraft` | `useActionState`の前状態、下記の入力項目、`applicationId`、`intent` | 成功時は`intent=confirm`なら`/confirm`、それ以外は`/edit?saved=1`。失敗時は`{ error, fields, fieldErrors }`を返して入力を保持 |
| 同上：`submitCampApplication` | `applicationId`、`confirmed=true` | `/complete`。受付番号をURLへ含めず、受付番号・提出日時・状態は完了ページでDBから取得する |
| `app/actions/guardian-consent.js`：`uploadGuardianConsent` | `applicationId`、ファイル入力 `guardianConsentFile` | `/edit?uploaded=1` |
| 同上：`createGuardianConsentDownloadUrl` | フォームではなく、引数に申請UUID文字列 | `{ error, url }` を返す。URLの有効期間は60秒 |

下書き保存の入力項目：`applicantName`、`applicantAddress`、`applicantPhone`、`emergencyContactName`、`emergencyContactAddress`、`emergencyContactPhone`、`usagePurpose`、`notes`、`guardianConsentRequired`、`requestedRoomPreference`。

相部屋希望は `shared_ok`（相部屋可）／`private_requested`（個室希望）。個室を保証する表示にはしません。同意書の要否は未成年者・18歳の高校生について確認し、チェック値は `on`／`true`／`1` を真として扱います。

### 4.2 エラーと表示データ

- 更新Actionは処理ごとに返却方式が異なります。認証画面や添付は主に`?error=...`で移動し、キャンプ申請の入力保存は`useActionState`用の`{ error, fields, fieldErrors }`を返して入力を保持します。キャンプ申請には`deadline-passed`・`not-eligible`・`capacity-full`・`required-fields`・`invalid-phone`・`guardian-consent`、添付には`file-required`・`invalid-size`・`invalid-type`・`invalid-content`・`upload-failed`などがあります。コード文字列をそのまま表示せず日本語の案内にします。
- T09のキャンプ・利用停止Actionは `{ error, fields, conflicts }` を返して入力を保持します（ルート設計9.4節）。T05の入力保存も同じ入力保持方式へ接続済みです。提出時の確認エラーは確認画面で日本語表示します。
- 本人向けキャンプ申請詳細は接続済みです。申請・納付・滞在の状態、公開理由だけの履歴、料金、現在の部屋を本人限定で取得します。その他の読取画面は、サーバー用SupabaseクライアントとRLSを使い、本人・職員の権限内で必要な列だけを取得します。更新は用意されたAction／RPCを使います。
- 申請状態、納付状態、滞在状態は別々に表示します。「申請済み」は「許可」ではありません。完了URLの受付番号だけを根拠に提出成功を表示しないでください。
- 接続に必要な変数名は `NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_ANON_KEY`。添付保存の管理用 `SUPABASE_SECRET_KEY` はサーバー専用です。値はこの文書に記載しません。

### 4.3 各機能の引き渡し完了チェック

- [ ] 対象URL、担当タスクID、利用できる役割が分かる。
- [ ] 入力項目・必須条件・値の形式・初期値が決まっている。
- [ ] Action／RPC名、取得データの項目、成功時の遷移、失敗時の案内が決まっている。
- [ ] 対応SQLの適用結果と必要な環境設定を共有している。
- [ ] 架空データで正常系・未ログイン・他人の情報・期限切れ・二重送信を確認している。
- [ ] 未完成の処理と、仮データで画面作成だけ進めてよい部分が分かる。
- [ ] 画面接続後に2人で完了条件を確認し、その結果を作業一覧に反映する。

### 4.4 キャンプの部屋割り・審査の接続（SQL 010）

対象は職員の `/staff/camps/[campId]/applications/[applicationId]` です。キャンプ個人だけを扱い、部屋は1申請1部屋・1人・キャンプの固定期間全体です。人数と日程をフォームから保存しません。地域活動・団体・利用停止期間との枠管理全体はT09以降の残作業です。

`updatedAt` には、画面を表示したときに取得した `applications.updated_at` の文字列をそのまま渡します。`new Date(...).toISOString()` 等でマイクロ秒を丸めたり、送信直前に最新の値へ置き換えたりしないでください。部屋割り成功後は親申請の更新日時も変わるため、最新の詳細を読み直してから許可します。SQL 010は旧3引数の `review_camp_application` を削除します。ActionとSQLをそろえずに接続すると審査を実行できません。

| RPC | 引数 | 返却列 |
|---|---|---|
| `assign_camp_application_room` | `target_application_id`、`target_room_id`、`expected_updated_at`、任意の `change_reason` | `result_camp_id`、`result_status`、`result_updated_at` |
| `review_camp_application` | `target_application_id`、`review_action`、`expected_updated_at`、任意の `public_reason` | 上記3列と `result_revision_due_at` |

審査操作は `start_review`・`request_revision`・`reject`・`approve` の4種類です。修正・不許可の理由、許可コメント、部屋変更理由は最大2,000文字です。初回の部屋割りは審査中に行い、理由は任意です。既存割当の部屋変更は審査中・許可後とも理由必須です。許可後は入居前・滞在中だけ変更でき、退去済みの割当は変更しません。修正依頼中は割当を維持し、不許可では `released_from = start_date` として全日を解放し、割当行と履歴を残します。

許可時は部屋・1人・全期間・未解放・日別定員を再確認し、申請状態、`stays.before_move_in`、状態履歴、監査履歴を同じトランザクションで保存します。部屋変更は料金・納付・滞在状態を変更しません。同じ部屋の保存は、更新日時が最新なら何も更新せず、履歴も追加しません。古い更新日時での再送は `stale-update` となり、許可や履歴を二重作成しません。

読取は既存RLSを使います。部屋一覧は `rooms` の `id,name,capacity`、本人の結果は `room_allocations` の部屋・人数・日程を取得します。状態履歴は本人向けに `from_status,to_status,public_reason,occurred_at` だけを返し、職員IDを含めません。職員専用の `audit_logs` は `entity_type='application'` と `entity_id=applicationId` で絞り、`action,before_data,after_data,reason,actor_user_id,occurred_at` を取得します。部屋の内部変更理由は利用者向け状態履歴へ転記しません。監査の追加対象は今回の職員操作だけで、他機能の監査はT15の残作業です。

主なエラーの日本語案内：

| エラー | 表示・次の操作 |
|---|---|
| `invalid-version`・`stale-update` | 最新の情報を再読み込みし、内容を確認してから操作してください |
| `room-required` | 許可する前に部屋を割り当ててください |
| `room-capacity-full` | 選択した期間の部屋定員を超えます。割当状況を確認してください |
| `facility-capacity-full` | 施設全体の定員15人を超えます。申請状況を確認してください |
| `reason-required`・`reason-too-long` | 理由を入力／理由またはコメントを2,000文字以内に修正してください |
| `invalid-status`・`stay-completed` | 現在の申請・滞在状態では操作できません |
| `invalid-room` | 部屋を選び直してください |
| `camp-unavailable`・`camp-dates-changed` | キャンプ設定が変更されています。最新の期間と申請内容を確認してください |
| `invalid-allocation`・`invalid-stay` | 割当・滞在情報が整合していません。管理担当者へ確認してください |
| `forbidden`・`not-found` | 職員権限または対象申請を確認してください |
| `invalid-action`・`update-failed` | 更新できませんでした。再読み込みして確認してください。内部情報は表示しません |

検証ファイル：[camp_room_allocations_and_approval.sql](../supabase/tests/camp_room_allocations_and_approval.sql)。更新後のファイルはSQL 001〜011を必要とします。検証用SupabaseプロジェクトでSQL 009→010→011を適用した後、SQL Editorの `postgres` ロールでファイル全体を1接続で実行します。成功時は検査結果一覧と件数を確認します。途中でエラーが出た場合はその検査を失敗と扱い、同じ接続で `ROLLBACK` してから原因を調べます。最後の `ROLLBACK` を `COMMIT` に変更したり、一部だけを選択して実行したりしないでください。検証中は施設ロックを保持するため、他の利用者が操作していない検証用環境を使います。

SQL 011に対応する検証として、期限前の下書き作成・再利用、未ログイン・停止中・対象外の拒否、採番・料金・割当・滞在の非作成、履歴の重複防止、期限後の既存下書き再利用・新規作成の拒否を追加しました。元の失敗ケースは維持しています。修正再提出の成功ケースの初回期限は、旧処理の `now()` でも確実に過去となるようトランザクション開始前へ設定し、旧実装へ戻った場合も検査で検出できるようにしました。更新後の検証SQLは218項目成功しました（ユーザー提供画像で確認）。

テストはDBの役割・セッション情報を設定してRPC・RLSを実行しますが、Authへの実ログインやJWT署名検証の代わりにはなりません。同意書は架空メタデータで検査するため、Storageの実ファイル検証はT06で別途必要です。SQL 009の期限を現在時刻に設定した拒否ケースは含みますが、`clock_timestamp()` と修正期限のマイクロ秒単位の完全一致や、ロック待ち中の期限超過を実証するものではありません。

DBでの検証項目（単一接続218項目と、第4.5節の複数接続7ケースは成功）：

- SQL 009の期限境界、再提出の受付番号維持、同意書・料金・履歴。初回期限後の修正再提出をSQL 008相当の審査フローから通す。
- 各部屋の定員ちょうどの割当と、次の1人の拒否。開始日・終了日を含む日別検査、別期間の同じ部屋の利用。
- 別キャンプを含む施設15人の検査、部屋未割当の申請・修正依頼中・キャンセル申請中の人数の算入。
- 未ログイン、一般利用者、停止中職員、他人の申請、部屋・申請・滞在・監査履歴への直接書込みの拒否。利用者による監査履歴読取の拒否。
- 下書きや申請済みからの直接許可、部屋未割当・日程不一致・解放済みの許可の拒否。正常な許可時の入居前・状態履歴・監査履歴の一致。
- 許可後の理由なし変更の拒否、理由あり変更の前後内容・操作者・日時、同室保存の履歴非増加、退去済み変更の拒否。
- 修正依頼中の割当保持、不許可の全日解放と履歴保持。エラー時に状態・割当・滞在・履歴が一部だけ更新されないこと。
- 複数DB接続で、残り1枠の部屋への同時割当は1件だけ成功すること。同じ申請への変更・許可の競合や二重送信は古い更新日時を拒否すること。マイクロ秒だけ異なる値も区別すること。

### 4.5 同時更新の検証（7ケース成功・後片付け確認済み）

[camp_room_allocations_concurrency.sql](../supabase/tests/camp_room_allocations_concurrency.sql) の修正版をSQL 001〜011適用済みの検証用プロジェクトで実行しました。A・B両方の「Success. No rows returned」をユーザーが確認した後、`select * from t12_concurrency_test.verify();` を実行しました。2026年9月10日19:47の提供画像で、ケース1〜7すべてが `passed = true`、接続IDはAが `100880`、Bが `100891` と確認できました。拒否コードはケース1〜4・6が `P0001`、ケース5が `40001`、ケース7が `42501` です。単一接続の218項目と区別して、複数接続での7ケース成功として記録します。

`verify()` はAがBを実際にロック待ちさせた接続ID・時刻、双方の分離レベル、拒否理由、失敗したBの部分更新がないことも検査します。対象ケースは下表のとおりです。Authの実ログイン、Storageの実ファイル、画面を通した操作はこの検証に含みません。`npm run lint`・`npm run build` の成功も、DBでの実行結果とは区別します。

初回はBがケース5の分離レベル変更でSQLSTATE `25001`、Aが相手の待機確認でタイムアウトしました。Bの条件分岐評価が先にスナップショットを取得する検証コードの問題として、分岐後・COMMIT直後に分離レベルを設定するよう修正しました。Aもループ評価後にCOMMITしてから設定し、実際の分離レベルの記録・検査を追加しています。DBの読み取り開始後には分離レベルを変更できません。[PostgreSQLのトランザクション設定](https://www.postgresql.org/docs/current/sql-set-transaction.html)、[条件式評価時のスナップショット取得](https://github.com/postgres/postgres/blob/REL_17_STABLE/src/pl/plpgsql/src/pl_exec.c)

初回失敗分は両接続のROLLBACK後、キャンプ7件・利用者25人・申請23件の削除結果と受付番号カウンター維持を確認しました。続く単独DO検証では専用スキーマの不存在と7回の分離レベル切り替えを確認しています。その後、修正版で再準備して今回の7ケースが成功しました。成功分も後片付けSQLを実行し、ユーザーがキャンプ7件・利用者25人・申請23件の削除結果と受付番号カウンター維持を確認しました。続いて `select to_regnamespace('t12_concurrency_test') is null as cleanup_completed;` を実行し、`true` で専用スキーマの削除も確認済みです。初回失敗分・成功分とも後片付けが完了しています。 適用済みのSQL 001〜011は検証コード修正時に変更していません。

ファイル全体の実行は準備だけです。`postgres` 専用の `t12_concurrency_test` スキーマと、架空の利用者25人（職員2人を含む）・キャンプ7件・申請23件を作ってコミットします。専用テーブルにはRLSを有効にし、スキーマ・テーブル・関数・プロシージャの権限を一般利用者・職員・service_roleへ開放しません。対象RPCだけを `authenticated` と架空の認証情報へ切り替えて呼びます。Auth・Storage API、パスワード、追加拡張は使いません。

準備後は別々のSQL Editorタブに次のSQLを用意します。1つのクエリには該当する `CALL` だけを置き、`BEGIN` で囲みません。Aを実行し、Aが実行中のまま30秒以内にBを実行します。各処理はケース間でコミットし、相手の待機・完了を確認しながら7ケースを進めます。[PostgreSQLのプロシージャとトランザクション制御](https://www.postgresql.org/docs/current/plpgsql-transactions.html)

```sql
-- 接続Aのクエリ
call t12_concurrency_test.worker_a();
```

```sql
-- 接続Bのクエリ
call t12_concurrency_test.worker_b();
```

| ケース | 接続A・Bの操作 | 確認する結果 |
|---|---|---|
| 1 | 別の申請を同じ1人部屋へ割当 | Aだけ成功し、Bは `room-capacity-full` |
| 2 | Aが部屋変更、Bが同じ申請を許可 | Aの変更を残し、Bは `stale-update` |
| 3 | 同じ申請を同時に許可 | 許可・入居前・履歴が1件で、Bは `stale-update` |
| 4 | 同じキャンプの有効申請14件に対し2件を同時提出 | 合計15人で止まり、Bには受付番号・料金を作らない |
| 5 | ケース2をBの `REPEATABLE READ` で実行 | 古いDBスナップショットはSQLSTATE `40001` |
| 6 | 修正期限前にBが再提出、Aのロックで期限後まで待機 | Bは修正期限切れ。元の申請・割当・受付番号・料金・履歴を維持 |
| 7 | Bの職員操作が待機中にAがその職員を停止 | 再検査で `42501 / staff-required`、更新・監査追加なし |

Aは `pg_blocking_pids()` で自分がBを待機させていることを確認し、双方のDB接続IDと時刻を記録します。検証時はその一致と、失敗したBによる部分更新がないことを確認します。処理内の待機には50秒の予算があり、競合が確認できない場合は成功と扱いません。SQL Editorがクエリを直列化したり `CALL` をトランザクションで囲んだりする環境では、この方法は使えません。その場合は環境側の失敗と区別し、2つの直接DB接続で実施する必要があります。[待機接続の取得](https://www.postgresql.org/docs/current/functions-info.html)、[監視情報の更新](https://www.postgresql.org/docs/current/monitoring-stats.html)

両方が終了したら `select * from t12_concurrency_test.verify();` を実行します。7行すべての `passed = true`、A・Bの異なる接続ID、期待する拒否コードを確認して結果を記録します。

結果確認後、準備の結果に表示される後片付けSQLを実行します。失敗時にも、両方の処理が停止したことを確認したうえで実行できます。

```sql
begin;
select t12_concurrency_test.cleanup();
drop schema t12_concurrency_test cascade;
commit;
```

後片付けは記録したUUID・架空メール・実行識別子を照合し、予期しない参照・添付・ログイン履歴があれば停止します。該当する架空の申請・関連行・キャンプ・利用者と専用スキーマだけを削除します。受付番号カウンターは再利用を避けるため巻き戻さず、テストで消費した番号は欠番になります。既存の業務関数・RLS・部屋マスターは変更しません。

途中で失敗した場合、エラーが出た接続で `ROLLBACK` し、両方が停止するまで待ってから後片付けします。片側だけを再実行せず、後片付け後にファイル全体から準備し直してください。専用スキーマが残っている場合、新しい準備は上書きせず停止します。

### 4.6 T09日程管理バックエンドの検証と引き渡し（SQL 012）

キャンプ・利用停止に限ったT09バックエンドの実装、**SQL 012のSupabase適用、単一接続328項目・別接続20ケースの検証、検証用データの後片付けが完了**。Supabaseはユーザーによる適用成功・各集約結果・両方の `cleanup_completed = true` の確認に基づく。ローカルPostgreSQL 18.4でも検証した。画面接続と実ログインでの受入確認は未実施。001〜011と既存の個人審査・部屋割当Actionは変更していない。通常処理は既存の利用者セッションを使い、新しい環境変数やSecret keyは不要。

| 確認 | 結果・範囲 |
|---|---|
| SQL 001〜012の順次適用 | ローカルで成功。移行前に有効・削除済みキャンプを作り、正しい日程と解放状態の移行を確認 |
| SupabaseへのSQL 012適用 | SQL Editorで「Success. No rows returned」をユーザーが確認。適用後の公開取得は表示確認済み。T09単一接続110項目も成功。既存218項目も成功。T09別接続13ケースも成功。T09後片付けも完了。既存7ケースもSQL 012適用後のSupabaseで成功。今回の既存7ケースの後片付けも `cleanup_completed = true` をユーザー確認済み |
| Supabase公開取得 | 読取専用トランザクションで `set local role anon` と当月の `get_public_calendar` を実行。ユーザーが表示範囲の `date / availability` 形式一致・エラーなしを確認。全30行の照合や画像確認はしていない |
| 移行前検査 | 有効キャンプの重複、提出済み申請の日程不一致、不正な期限を拒否し、SQL 012の途中状態が残らないことを確認 |
| 既存の単一接続テスト | `camp_room_allocations_and_approval.sql` の218項目がSQL 012適用後のローカル・Supabaseで成功。Supabaseは `passed_checks = 218` というユーザー確認に基づく。準備・終了時の枠対応だけを更新 |
| T09単一接続テスト | `calendar_and_blocked_periods.sql` の110項目がローカル・Supabaseの両方で成功。Supabaseは `passed_checks = 110` というユーザー確認に基づく。重複端点、隣接日、うるう年、月末・年末、JST受付境界、権限、公開情報制限、更新日時、下書き同期、削除、監査・枠保存失敗時の全体取消 |
| 既存の別接続テスト | `camp_room_allocations_concurrency.sql` の7ケースがSQL 012適用後のローカル・Supabaseで成功。Supabaseは `verify()` 集約結果 `checked_cases = 7 / all_passed = true` をユーザーが確認。別接続・実待機も関数内で検査。ローカル・Supabaseの後片付けも完了。Supabaseは `cleanup_completed = true` をユーザー確認済み |
| T09の別接続テスト | `calendar_concurrency.sql` の13ケースがローカル・Supabaseで成功。Supabaseは `verify()` の集約結果 `checked_cases = 13 / all_passed = true` をユーザーが確認。後述の競合と失敗側の部分更新なしを検査する。ローカル・Supabaseの後片付けも完了。Supabaseは `cleanup_completed = true` をユーザー確認済み |
| Action・取得処理 | `tests/calendar-actions.test.mjs` の27テスト成功。認可、RPC引数、日時精度、期限変換、入力保持、公開・職員の返却項目、失敗時の自動再試行なしを確認 |
| コード検査 | `npm run lint`・`npm run build`・`git diff --check` 成功。ビルド対象の業務画面はまだ存在しないため、フォーム操作の検証とは区別する |

ローカルのDB検証は一時フォルダの検証用PostgreSQLを使用し、実SupabaseのAuth・JWT署名・Storageを利用していない。Authスキーマの必要部分とセッション情報を代替した検証である。単一接続は架空データと検証トリガーをROLLBACKし、同時更新は専用スキーマ・架空データを削除した。受付番号カウンターは巻き戻さない。移行前検査の実行コードは一時環境の `run-local-postgres.mjs` に置き、プロジェクトの依存パッケージを追加していない。

Supabaseでは準備用SQLの成功後、A・Bの再実行が `already attempted: clean up before retrying`（P0001）で拒否されたとユーザーが報告した。状態確認時に準備用SQLを誤って貼り付けたが、実行前の警告でキャンセルしたとの報告あり。その後、`select count(*) as checked_cases, bool_and(passed) as all_passed from t09_concurrency_test.verify();` の結果が **`13 / true`** とユーザーが確認した。`verify()` はA・Bの完了、異なる接続ID、実待機、期待する拒否・整合性を検査するため、初回実行が成功していたと判断できる。DB接続IDの具体値・個別13行は取得していない。**その後、専用cleanup関数とスキーマ削除を含むSQLを実行し、`cleanup_completed = true` をユーザーが確認した。今回のT09後片付けは完了。** 架空データは専用関数の識別子・参照検査後に削除し、受付番号カウンターは維持する。削除件数の具体値は今回取得していない。

SQL 012適用後の既存7ケースについても、準備成功、A・B両方の実行成功をユーザーが確認した。続く `t12_concurrency_test.verify()` の集約結果が **`checked_cases = 7 / all_passed = true`** とユーザーが確認し、同環境での7ケース再検証も成功。接続IDの具体値・個別7行は取得していない。**続いて専用cleanup関数とスキーマ削除を含むSQLを実行し、`cleanup_completed = true` をユーザーが確認した。今回の `t12_concurrency_test` と架空データの後片付けも完了。** 削除件数の具体値は取得していない。第4.5節のSQL 001〜011時点の検証とは別の実行分として記録する。

T09の同時更新は次の13ケース。A・Bの異なる接続ID、`pg_blocking_pids()` による実待機、待機後の結果を検査する。

| ケース | 操作 | 期待結果 |
|---|---|---|
| 1 | 同じ日程へ利用停止を2件登録 | 後続は日程競合 |
| 2・3 | キャンプと利用停止を双方の順序で登録 | 後続は日程競合 |
| 4 | 同じ利用停止を2人が編集 | 後続は `stale-update` |
| 5 | 同じキャンプを編集・削除 | 後続は `stale-update` |
| 6 | キャンプの移動先へ利用停止を登録 | 新しいキャンプ枠との競合を拒否 |
| 7 | 申請提出の後でキャンプ削除 | `camp-has-applications` |
| 8 | キャンプ削除の後で申請提出 | 対象キャンプ不存在で拒否 |
| 9・13 | キャンプ日程変更と下書き再利用／新規作成 | 新しい日程を返す。採番・料金なし |
| 10 | 申請期限変更と待機中の提出 | 取得後の現在時刻と変更後の期限で拒否 |
| 11 | REPEATABLE READで日程競合 | SQLSTATE `40001` |
| 12 | 日程更新待機中の職員停止 | SQLSTATE `42501` |

再現用SQLはSQL 001〜012を適用したテスト環境で、`postgres` 権限を使用する。単一接続のファイルは全体実行する。同時更新のファイル全体は準備用であり、その後、別接続から `call t09_concurrency_test.worker_a();` と `call t09_concurrency_test.worker_b();` を実行する。両方の完了後に `select * from t09_concurrency_test.verify();` で13行すべてを確認し、ファイルに記載した実行分専用の後片付けを行う。詳細な待機・再実行の条件は同ファイル冒頭と第4.5節の方式に従う。ユーザーと実行する際は、その時点の操作を1つずつ案内する。

Actionテストは次で再実行できる。Next.js・認証・DB境界だけを代替し、実際のActionと取得処理のソースを読み込む。DBテストとは別の確認である。

```bash
node --experimental-vm-modules --test tests/calendar-actions.test.mjs
```

フロント担当へ渡す入力・返却項目・成功時遷移・エラー表示は [ルート設計9.4節](routes.md#94-t09バックエンドの接続契約sql-012) にまとめた。キャンプ作成のエラーがクエリ遷移から `{ error, fields, conflicts }` 返却へ変わる点と、`updatedAt` のマイクロ秒保持に注意する。SQL 012と更新後のコードをそろえて接続する。

残作業は実ログイン・画面からの動作確認、T10の個人枠、T18以降の団体枠との統合。公開カレンダーの「申請可能」は日単位の案内であり、地域活動申請の受付開始を意味しない。T09全体は完了にしない。

## 5. 接続前に解消・検証する設計との差分

これは今回見つけた残作業です。静的な確認から挙げたものを含み、すべてが実環境で再現確認済みの不具合という意味ではありません。

| 関連タスク | 現在の実装と設計との差分 | 次に行うこと／完了の判断 |
|---|---|---|
| T01・T04・T05 | SQL 003の内部提出処理とSQL 004のキャンプ作成処理にあった、結果を受け取らないロック用 `SELECT` はSQL 007で `PERFORM` へ修正・適用済み | 職員のキャンプ作成と利用者の提出を画面から実際に呼び出して確認する |
| T02 | Proxyはセッション更新のみ。利用者・職員layoutと各データ取得・Actionで認可する。ログアウトは設計どおり公開トップ `/` へ戻る | 架空の一般利用者・職員・停止中アカウントで実ログインし、URL直接入力と役割別遷移を確認する |
| T02・T14・T22 | 連絡先はログイン済み全員に直接SELECT可能。プロフィール等の読取も設計のactive状態・表示条件との照合が必要 | DB設計9章の条件付き取得に合わせ、停止／初期化中のアクセスと連絡先の取得範囲を確認する |
| T04 | SQL 012対応Actionで指定分の終わりを含む期限変換と、キャンプ開始日の00:00以下の検査を追加。既存期限データは変更しない | ローカル検証済み・SQL 012適用済み。表示はDB境界の1分前を日本時間へ変換して画面から確認する |
| T05 | 入力値保持、項目別エラー、プロフィール再利用、提出確認同意は画面へ接続済み。保存・提出Actionには画面取得時の `updated_at` 比較がない | 古い画面からの保存・提出をDBで拒否できるよう版照合を追加し、実ログインで二重送信と修正再提出を確認する |
| T05 | SQL 009に修正再提出の期限判定修正を追加。Supabase適用成功はユーザー確認済み。単一接続で初回期限後の修正再提出を検証済み | 単一接続218項目で初回期限後の修正再提出、期限超過・期限未設定・日程変更の拒否、同意書メタデータ、受付番号・料金・履歴を検証済み。同時更新ケース6でロック待ち中の修正期限超過も拒否できた。残る画面接続・実ログイン・実ファイルでの受入確認へ進む |
| T05 | 検証SQLの実行で、初回期限後の下書き作成入口の拒否検査が失敗（ユーザー報告）。SQL 003の `now()` を `clock_timestamp()` へ変更するSQL 011を作成・適用済み（適用成功はユーザー報告） | 元の失敗ケースと追加ケースを含む単一接続の218項目は成功。下書き保存・同意書登録にも残る `now()` と修正期限未設定時の扱いは別途点検する |
| T05 | 下書きUUIDはDBで生成し、同一利用者・キャンプの既存申請を再利用する。設計の初回UUID再利用方式と異なる | 現方式の二重送信時の挙動を確認し、全区分に広げる際の作成契約を統一する |
| T06 | 認証利用者が同意書メタデータ登録RPCを直接呼べる問題は、SQL 007でサーバー専用権限へ変更・適用済み。通常利用者に実行権限がないことも確認済み | Server Actionからの実ファイル登録、差し替え、提出だけが成功することを画面から確認する |
| T06 | 非公開 `guardian-consents` バケット作成はSQL外。差し替え後の古いファイル削除失敗の確認も残る | バケット非公開・直接操作禁止・保存／差し替え／失敗時処理を実環境で確認する |
| T09・T10・T12・T18 | SQL 013でcamp／community_individualと個人枠へ対応済み。割当は1申請1部屋・1人。SQL 014に地域活動個人の割当・許可を追加（Supabase適用・DB検証・後片付け完了）。団体の表・枠は未実装 | SQL 014適用・Supabase回帰検証は完了。第4.8節・ルート設計9.6節の契約で個人の部屋割当・許可画面を接続する。団体はT18以降で追加する |

## 6. おすすめの進め方と完了の扱い

1. **環境と既存処理を確認する**：T01、第5章のSQL実行・同意書の課題を先に確認する。フロントは並行してT07と認証画面の枠組みを作れる。
2. **キャンプで最初の一連の操作をつなぐ**：T02〜T08で職員登録→利用者提出までをつなぎ、T11〜T15で審査・部屋割り・納付・入退去まで進める。
3. **日程管理と地域活動を追加する**：T09〜T10、T16〜T17を進める。定員・日程整合性の確認が終わるまで複数利用者の受付を公開しない（DB設計12章）。
4. **団体と自動処理を追加する**：T18〜T22を進め、専有枠・個人情報の制限・期限処理を組み合わせて確認する。
5. **全体を受け入れ確認する**：T23〜T24で3つの利用区分を通し、権限・同時更新・日付境界も確認する。T25は本運用前に完了する。

各タスクを更新するときは、状態・担当・残作業・検証結果・フロントへの引き渡し状況を一緒に更新します。機能全体を「実装済み」に変更するのは、その行の完了条件を画面と保存処理の両方で確認してからです。未確認なら「コードあり・動作未確認」と残してください。

### 必須作業に混ぜないもの

| 区分 | 対象 | 状態・担当・完了条件・引き渡し |
|---|---|---|
| 時間があれば追加 | パスワード再設定、修正依頼・許可・不許可メール、検索結果CSV | 未着手／共同。必須機能の後に実施判断。再設定は関連3ルートを一組で検証し、通知は要件21章の宛先制限、CSVは町への項目確認後に接続する |
| 試作版の対象外 | オンライン決済、正式通知書・納付書PDF生成、招待メール、期限前通知メール、利用者自身のアカウント削除など | 未実装でも必須作業の未完了には数えない。対象範囲は要件4.3・21・25章を参照する |

この一覧は作業のたびに更新します。完了した作業だけを「実装済み」とし、コード作成・DB適用・画面接続・動作確認のどこまで終わったかを区別して記録します。


### 4.7 T10地域活動個人の実装と検証（SQL 013）

2026年9月11日、`codex/community-individual-applications` で承認済み範囲のバックエンド実装、SQL 013のSupabase適用、DB検証、検証用データの後片付けが完了。**ローカル・Supabaseとも単一接続558項目（T10の230＋カレンダー110＋キャンプ218）、別接続40ケース（T10の20＋キャンプ7＋カレンダー13）が成功。Supabaseの各集約結果と3つの `cleanup_completed = true` はユーザー確認に基づく。** 画面・実ログイン・実Storageの受入確認は未実施のため、T10全体は「作業中」とする。SQL 001〜012・要件定義書は変更していない。

実装は地域活動の個人に限る。職員操作は修正再提出の確認に必要な「審査開始・修正依頼・不許可」まで。キャンセル・継続／延長、許可・部屋割り、納付状態の更新、入退去、団体、Cronは対象外。既存の `cancellation_requested / approved` 状態を定員計算に含めるが、その状態へ変更するT10の操作は公開しない。

| 作成したファイル | 目的 |
|---|---|
| `supabase/migrations/202609100013_community_individual_applications.sql` | 個人申請・枠の制約、RPC、権限、カレンダー統合、料金・採番・監査 |
| `app/actions/community-applications.js` | 本人の下書き作成・保存・提出／再提出 |
| `app/actions/staff-community-applications.js` | 職員の最小審査操作 |
| `utils/community-applications/validation.js` | 入力名・DB列名の対応、構造検査、安定したエラー |
| `utils/community-applications/queries.js` | 本人一覧・編集・確認・完了・詳細用の読取 |
| `tests/community-applications-actions.test.mjs` | 実モジュールを使ったAction・取得・添付・認証の境界テスト |
| `supabase/tests/community_individual_applications.sql` | 架空データを末尾でROLLBACKする単一接続検証 |
| `supabase/tests/community_individual_applications_concurrency.sql` | 異なる2接続の実待機・競合・後片付けを検証 |
| `scripts/test-community-applications-db.mjs` | 外部のローカルPostgreSQLランタイムを使う一時DB検証。環境設定ファイルやホストDB接続情報を読まない |

既存変更は `auth.js` の登録後の安全な日程復元、`profile.js` の作成画面再検証、`guardian-consent.js` の本人・active・更新日時検査と提出済み旧ファイル保持、カレンダーのJS／SQLテスト、4文書（本書・DB・ルート・AI概要）。新しい環境変数やプロジェクト依存パッケージの追加は不要。

**日付と修正の扱い**：DBが施設ロック取得後の `clock_timestamp()` から日本時間の当日を求める。初回提出と日程変更再提出は開始≧当日+14、終了≦当日+60、終了−開始+1が2〜15日。下書きは未入力を許し、入力済み日付の実在・順序・長さを検査する。確認取得でも受付窓・空き・必須事項を確認し、提出で改めて検査する。同日程の修正は初回受付窓を再適用しない。個人の修正期限は任意で、指定された場合だけ期限未満を要求する。

修正中の候補日程は別列に保存するため、保存だけでは元の枠・料金を変更しない。再提出成功時だけ日程・枠・料金を入れ替える。失敗時はすべて維持する。不許可は枠を全解放し、番号・料金・過去の監査を残す。提出済み状態は利用許可ではない。

**料金との接続**：両端を含む日数×300円、月別上限9,000円、申請ごとに計算する。初回・日程変更再提出だけ確定額を保存する。同日程再提出は料金行・内訳・納付情報を維持し、日程変更でも納付状態・期限を書き換えない。料金と日程の前後内容は職員監査に残す。15日上限の個人申請に、料金ヘルパー検証用の長期間を提出できるようにはしない。

| 検証 | 結果・範囲（環境を区別） |
|---|---|
| SQL 001〜013の順次適用 | PostgreSQL 18.4の空DBに適用成功。Authの必要表・関数はローカル代替実装 |
| 単一接続 | 558項目成功：既存218、カレンダー110、T10の230。各SQLの末尾でROLLBACK |
| T10単一接続の主な内容 | 下書き・提出・修正・再提出・不許可、13／14日前・60／61日・1／2／15／16日の境界、JST日付切替、うるう年、必須項目、15人定員、日程端点、本人と他人・非active・職員の権限、直接更新拒否、同意書・監査、GETの無更新、料金例、途中失敗時の全体取消 |
| 別接続 | 40ケース成功：既存7＋13、T10の20。実待機・分離レベル・接続ID・失敗側の部分更新なしを検証 |
| T10の20ケース | 最後の1枠、本人の重複申請、同一キー再送、同一版の保存、修正保存と再提出の両順序、個人とキャンプ／利用停止の作成・編集の各両順序、REPEATABLE READ、待機中の本人停止・職員停止・修正期限超過、同意書更新と提出、職員の同時判断 |
| 後片付け | 3つの同時操作テストの専用スキーマと架空データを削除し不存在を確認。一時DB自体も終了・削除 |
| Nodeテスト | 74テスト成功。Next・認証・DB・Storageの境界だけを代替し、実モジュールの認可順序・入力保持・RPC・リダイレクト・公開情報制限を検証 |
| lint・build | 成功。buildは最初に既存Google Fonts取得の通信制限で失敗し、通信許可後の再実行で成功 |
| SupabaseへのSQL 013適用 | 2026年9月11日、SQL Editorの警告内容を確認後に実行し、適用成功をユーザーが報告。適用後の単一接続558項目・別接続40ケースと全後片付けも、下記の集約結果でユーザー確認済み |
| Supabase事前確認 | ユーザーが `sql_013_ready = true / existing_claims = 1` を報告。当初は対象期間の既存枠を確認するため、230項目のテストを保留した。提供画像で `camp / 2026-09-24 / 2026-09-30 / released_from=NULL` の1行を確認。続く画像で申請合計・下書き・有効件数がすべて0と確認。キャンプ名は画像で「MVP動作確認」まで読み取れる。申請0件のこのキャンプを論理削除して枠を解放することをユーザーが承認。対象IDは `4391b1ac-f608-45b4-bf8c-fbc9c43d764d`、確認した更新日時は `2026-09-10 03:35:27.995009+00`。管理SQLは施設ロック・版・日程・申請0件・枠一致を再検査し、論理削除と枠解放・監査を一括保存する。ローカルの架空データで正常系、再実行、競合、新規申請、権限、監査失敗時の全体取消を検証済み。監査はAuth利用者を代入せずsystem＋SQL Editor管理操作と明記する。Supabaseで `camp_deleted = true / claim_released = true / application_count = 0` をユーザーが確認し、承認されたキャンプの論理削除・枠解放が完了。続くT10単一接続230項目も成功をユーザー確認済み |
| Supabase T10単一接続 | `community_individual_applications.sql` の全文を実行し、`passed_checks = 230` をユーザーが確認。作成する表は接続専用の一時表であり、既存RLSを無効化しないため、SQL Editorの警告は「Run without RLS」で実行。末尾のROLLBACKで検証中の変更を取消。実Auth・Storageの検証とは区別する |
| Supabaseカレンダー単一接続（013適用後） | 更新後の `calendar_and_blocked_periods.sql` 全文を実行し、`passed_checks = 110` をユーザーが確認。末尾のROLLBACKで検証中の変更を取消。SQL 012時点の検証とは別の実行結果 |
| Supabaseキャンプ単一接続（013適用後） | `camp_room_allocations_and_approval.sql` 全文を実行し、`passed_checks = 218` をユーザーが確認。末尾のROLLBACKで検証中の変更を取消。これで013適用後の単一接続は合計558項目成功 |
| Supabase T10別接続 | 準備後、A実行中にBを開始し、両方の終了成功をユーザーが確認。続く `verify()` の集約結果 `checked_cases = 20 / all_passed = true` もユーザー確認済み。別接続・実待機・失敗側の部分更新なしを関数内で検査。個別行や接続IDの出力は取得していない。両接続終了後に専用の `cleanup()` とスキーマ削除を実行し、COMMIT後の `cleanup_completed = true` をユーザーが確認。専用スキーマと架空データの後片付けは完了。受付番号カウンターは維持 |
| Supabaseキャンプ別接続（013適用後） | 準備後、A実行中にBを開始し、両方の終了成功をユーザーが確認。続く `t12_concurrency_test.verify()` の集約結果 `checked_cases = 7 / all_passed = true` もユーザー確認済み。別接続・実待機も関数内で検査。個別行や接続IDの出力は取得していない。両接続終了後に専用の `cleanup()` とスキーマ削除を実行し、COMMIT後の `cleanup_completed = true` をユーザーが確認。専用スキーマと架空データの後片付けは完了。受付番号カウンターは維持 |
| Supabaseカレンダー別接続（013適用後） | 準備後、A実行中にBを開始し、両方の終了成功をユーザーが確認。続く `t09_concurrency_test.verify()` の集約結果 `checked_cases = 13 / all_passed = true` もユーザー確認済み。別接続・実待機も関数内で検査。個別行や接続IDの出力は取得していない。両接続終了後に専用の `cleanup()` とスキーマ削除を実行し、COMMIT後の `cleanup_completed = true` をユーザーが確認。専用スキーマと架空データの後片付けは完了。受付番号カウンターは維持 |
| 実ログイン・Storage・ブラウザ | 未実施。上記のローカル・SQL検証結果から成功とは扱わない |

ローカル再現は `node --experimental-vm-modules --test tests/*.test.mjs`、`npm run lint`、`npm run build`。DBは `node scripts/test-community-applications-db.mjs`。DBランナーはプロジェクト外にある `embedded-postgres@18.4.0-beta.17` と `pg@8.23.0` を使う。既存配置の既定値は `/private/tmp/hiraizumi-t09-postgres`。別配置ならそのパッケージディレクトリを `T10_DB_RUNTIME` に指定する。一時ランタイムが削除済みなら先にランタイムの再準備が必要。ランナーは外部DBや `.env.local` を使わず、ランダムポート・一時パスワードで127.0.0.1にのみ接続する。`--single-only`、`--migrate-only`、既知の検証SQLファイル名を指定して絞り込める。

**Supabaseでの確認状況と残作業**：SQL 013適用、単一接続558項目・別接続40ケース、3種類のテスト用スキーマと架空データの後片付けが完了。DBの検証手順は一通り終了した。残る受入確認は、画面接続後の実ログイン・実Storageを含む一連の操作。以下は再実行用の記録であり、今回分を再実行する必要はない。単一・別接続の回帰検証は、利用者の操作がなく、JST当日+14〜60日の枠が空いている専用の検証用プロジェクトで行う。本番・運用データがある環境に架空データを入れない。

単一接続用の `community_individual_applications.sql` は全体をSQL Editorのpostgresで実行し、`passed_checks = 230` が想定結果。末尾のROLLBACKを変更しない。失敗時は同じ接続でROLLBACKして原因を確認する。カレンダー検証は更新後のファイルが013必須で、110項目。既存キャンプは218項目。

別接続用ファイルは準備をCOMMITする。準備後、別々の接続で `call t10_concurrency_test.worker_a();` と `call t10_concurrency_test.worker_b();` をAが実行中に開始する。両方終了後、`select count(*) as checked_cases, bool_and(passed) as all_passed from t10_concurrency_test.verify();` の想定結果は `20 / true`。同時CALLとトランザクション制御をSQL Editorが許さない場合は、検証用DBへの2つの直接接続が必要。準備だけの成功や、時間差実行を競合検証成功とは扱わない。

成功・失敗とも両接続終了後に、`begin; select t10_concurrency_test.cleanup(); drop schema t10_concurrency_test cascade; commit;` で専用の架空記録を削除する。続く `select to_regnamespace('t10_concurrency_test') is null as cleanup_completed;` はtrueが想定結果。受付番号カウンターは再利用防止のため維持する。再試行は後片付け後に準備から行う。これらは再現方法の記録であり、初心者への実行案内は想定結果を説明して1操作ずつ行う。

画面接続後には、架空アカウントでログイン、新規登録後の日程復元、期限・満員時の入力保持、必要な同意書の実アップロード・差し替え・権限付き取得、職員修正依頼から本人再提出までを確認する。Storage通信の失敗や孤立ファイルの後片付けはT06で別途確認する。

SQL Editorに作成した今回のテスト用クエリ（単一接続・準備・A・B・集計・後片付け）は、結果を本書に記録済みのため削除してよい。SQL 013適用用と、承認された空キャンプの論理削除用の管理クエリは作業記録として保存する。プロジェクト内のマイグレーション・テストSQLファイルは保持する。


### 4.8 T12地域活動個人の部屋割当・許可（SQL 014）

2026年9月11日、`codex/community-individual-room-approval` で承認済みの既存10ファイル変更・新規4ファイル作成を実施。**バックエンド実装、SQL 014のSupabase適用、ローカル・Supabaseの単一接続916項目・別接続60ケースの検証、4種類のテスト用データとスキーマの後片付けが完了。** Supabaseの適用・各集約結果と4つの `cleanup_completed = true` はユーザー確認に基づく。画面接続・実ログイン・実Storageは未実施のため、T12全体は「作業中」とする。SQL 001〜013、`requirements.md`、無関係な `docs/apple_event.txt` は変更していない。秘密情報の表示・コミットは行っていない。Supabase操作はユーザーがSQL Editorで行い、想定結果への「y」の返信を確認根拠としている。バックエンドは引き渡し可能で、本ブランチの変更は未コミット・main未反映。適用済みSQL 001〜014は以後書き換えず、DB修正は新しいマイグレーションを追加する。

| 新規ファイル | 目的 |
|---|---|
| `supabase/migrations/202609110014_community_individual_room_allocations_and_approval.sql` | 個人部屋割当、許可、日別定員・枠整合、監査の滞在追加、本人／職員の結果取得、明示的な権限 |
| `supabase/tests/community_individual_room_allocations_and_approval.sql` | 末尾ROLLBACKの単一接続358項目。実提出から許可・再割当、権限・整合性・失敗時全体取消 |
| `supabase/tests/community_individual_room_allocations_concurrency.sql` | 専用スキーマで2接続の実待機を証明する20ケース。検証と限定された後片付け |
| `tests/individual-room-allocations-actions.test.mjs` | 新Action・取得、既存キャンプActionを含む38テスト。実モジュールを読み込み、認証・Next・DB境界だけを代替 |

既存変更は職員Action、地域活動のvalidation／queries、T10のNode／SQLテスト、ローカルDBランナー、AI概要・本書・DB設計・ルート設計。利用者の下書き／提出Action、キャンプAction・RPC、カレンダーActionは変更しない。新しい環境変数やプロジェクト依存パッケージは不要。

追加Actionは `assignCommunityApplicationRoom` と `approveCommunityApplication`。全操作で職員権限・元の更新日時を必須とし、保存時にDBで再検査する。1申請1人1部屋・提出済み利用期間全体を使い、提出時に確保済みの施設枠を二重計上しない。初回割当は審査中、既存変更は審査中／許可後の入居前・滞在中、退去済みは拒否する。既存変更・再割当は2,000文字以内の理由必須、初回割当理由と許可コメントは任意。最新版の同内容は無更新、古い版は同内容でも拒否。許可時に状態・入居前滞在・公開履歴・職員監査を一括確定する。

修正依頼と同日程再提出では部屋を維持する。SQL 013の日程変更再提出は旧割当を全解放し、014は再審査中の明示的な再割当を許可する。同じ部屋でも新期間への割当には理由が必要。旧行を更新する前後の期間・部屋・解放状態を `reassign_room` 監査へ残す。割当変更で料金・納付・許可期間・滞在状態を変えない。本人取得の `room_allocation.is_current=false` は旧割当であり、現在の部屋と表示しない。契約は [routes.md 第9.6節](routes.md#96-t12地域活動個人の部屋割当許可sql-014)。

| ローカル検証 | 結果 |
|---|---|
| SQL順次適用 | PostgreSQL 18.4へ001〜014の適用成功 |
| 013→014の既存データ保持 | 013の実RPCで作ったキャンプ許可・地域活動修正中の申請を含む16テーブルの保存内容が完全一致。採番・料金・滞在・監査・部屋等を保持。架空データ後片付けも成功 |
| 単一接続 | 916項目成功：キャンプ218＋カレンダー110＋地域活動申請230＋新規部屋割当358。各ファイル末尾でROLLBACK |
| 新規単一接続の主な内容 | 実提出→審査→割当→許可、8部屋の定員ちょうど／超過、期間途中と両端日、15人時の二重計上防止・超過の再検査、日程変更後再割当、同日程修正、許可後変更、退去済み拒否、不許可の両枠解放、必須・同意書・枠・料金不整合、権限・RLS・取得の無更新、監査失敗時の全体取消 |
| 別接続 | 60ケース成功：キャンプ7＋カレンダー13＋地域活動申請20＋新規20。各verifyで別接続IDと実際の待機、拒否側の部分更新なしを検査 |
| 後片付け | 4種類とも専用のcleanupとスキーマ削除・不存在検査が成功。一時DBも停止・削除。受付番号カウンターは実行中の再利用防止のため維持 |
| Node | 112テスト成功（既存74＋新規38）。入力保持・認可・エラー非開示・RPC・版精度・再検証／遷移・本人／職員の固定返却列 |
| lint・build | 成功。Next.js 16.3.4のローカルServer Actions／use server／revalidatePath／redirect資料を事前確認 |
| 検証範囲 | SQLテストのAuth／JWTは代替であり、実ログイン・JWT署名・Storage APIの成功と扱わない。画面・実ログイン・実Storageの受入確認は未実施 |

新規別接続20ケース：残り1部屋枠、部屋変更→許可、許可→部屋変更、二重許可、許可前二重部屋変更、許可→不許可、不許可→許可、修正依頼→許可、許可→修正依頼、日程変更後の二重再割当、不許可→部屋変更、同じ部屋の同時無更新保存、割当済みの人を含む施設最後の1枠、REPEATABLE READ、待機中の職員停止、待機中の職員権限削除、部屋保持中の修正期限超過、割当と競合キャンプ作成、許可と競合利用停止編集、許可後二重部屋変更。

**再現方法（記録用。ユーザーへの実行案内は1操作ずつ）**

ローカルは `node --experimental-vm-modules --test tests/*.test.mjs`、`npm run lint`、`npm run build`。DB全体は `node scripts/test-community-applications-db.mjs`。`--migrate-only` は移行と既存データ保持だけ、`--single-only` は単一接続まで。上表の新規SQLファイル名を引数に指定して個別実行もできる。ランタイムは第4.7節と同じ外部の `embedded-postgres / pg`、既定配置 `/private/tmp/hiraizumi-t09-postgres`、必要時は `T10_DB_RUNTIME` で指定。環境設定ファイルとホストDB接続情報を読まず、127.0.0.1のランダムポートの一時DBだけを使用する。ファイル作成だけでSupabase適用済みと扱わない。

Supabaseの検証は、利用者の操作がなく14〜60日後の枠が空いている検証用プロジェクトで行う。運用データを持つ環境に検証用データを入れない。SQL 014の適用前確認、全文の適用、新規2関数の存在確認、新規単一接続テストの `passed_checks = 358` はユーザー確認済み。新規テストのRLS警告は一時テーブル3つに対するもので、全文の末尾ROLLBACKを維持して「Run without RLS」で実行した。既存地域活動個人申請テストの `passed_checks = 230` もユーザー確認済み。カレンダーテストの `passed_checks = 110` もユーザー確認済み。キャンプテストの `passed_checks = 218` もユーザー確認済みで、単一接続4ファイルの916項目が完了。続く別接続4ファイルも検証・後片付けが完了した。キャンプ／カレンダー／T10の既存回帰を含む。

新規の単一接続は `community_individual_room_allocations_and_approval.sql` の全文をSQL Editorのpostgresで実行し、`passed_checks=358` を確認。末尾ROLLBACKは維持する。失敗時は同じ接続でROLLBACKして原因を確認する。

SQL 014適用後のSupabase別接続の結果は以下のとおり。4種類とも準備後にA実行中のB開始・両方の正常終了をユーザーが確認し、verifyで別接続・実待機・失敗側の部分更新なしを検査した。個別行・接続IDの出力は取得していない。両接続終了後に専用のcleanup・スキーマ削除を実行し、COMMIT後のスキーマ不存在を確認した。

| テスト | 専用スキーマ | ユーザー確認の集約結果 | 後片付け確認 |
|---|---|---|---|
| 新規地域活動個人の部屋割当・許可 | `t12i_concurrency_test` | `20 / true` | `cleanup_completed = true` |
| 既存地域活動個人申請 | `t10_concurrency_test` | `20 / true` | `cleanup_completed = true` |
| 既存キャンプ個人 | `t12_concurrency_test` | `7 / true` | `cleanup_completed = true` |
| 既存カレンダー・利用停止 | `t09_concurrency_test` | `13 / true` | `cleanup_completed = true` |

全60ケースの成功と4種類の後片付けを記録済み。受付番号カウンターは維持しており、今回の準備SQL・A・B・後片付けの再実行は不要。既存地域活動個人申請の削除結果は、利用者70人・申請68件・キャンプ3件・利用停止3件、受付番号カウンター維持（ユーザー提供JSON）。以下の手順は再現用の記録であり、現在の再実行指示ではない。

新規の別接続は `community_individual_room_allocations_concurrency.sql` 全文で準備をCOMMITする。その後、別の接続でAの `call t12i_concurrency_test.worker_a();` を開始し、A実行中にBの `call t12i_concurrency_test.worker_b();` を開始する。CALLは単独、BEGINで囲まない。両方終了後に `select count(*) as checked_cases, bool_and(passed) as all_passed from t12i_concurrency_test.verify();` で `20 / true` を確認する。別タブが実際には同じ接続・直列実行となる環境では、検証用DBの別々の直接接続が必要。準備成功や時間差実行だけを同時操作成功と扱わない。

成功・失敗とも両接続終了後に `begin; select t12i_concurrency_test.cleanup(); drop schema t12i_concurrency_test cascade; commit;` で専用データだけを片付ける。次に `select to_regnamespace('t12i_concurrency_test') is null as cleanup_completed;` でtrueを確認する。清掃は架空ユーザーのUUID・実行マーカー・想定外参照を確認してから行う。受付番号カウンターは戻さない。再試行は後片付け後に準備から行う。

ユーザー操作は想定結果を説明して1つずつ案内し、SQL Editorでは毎回「New query／既存クエリ置換」と「保存／確認後に削除可」を明記する。014適用クエリは保存。今回のテスト用クエリは検証・後片付け・記録が完了したため削除可。プロジェクトのマイグレーション・テストファイルは保持する。

今回の対象外はキャンセル・延長・納付更新・入退去・早期退去・団体。テスト内の状態変更用fixtureは製品APIではない。Supabase検証は完了しており、後続では業務画面接続・実ログイン・実Storageの受入確認を行う。

### T13 Phase 1：納付操作（2026年9月11日・ローカル実装済み）

承認範囲はPhase 1だけ。納付期限設定／解除、未納／納付済み更新、未納差戻し理由、納付更新の監査、本人・職員の取得、JST期限超過フラグを追加。既存申請Actionの入力契約は維持。Phase 2の入退去とPhase 3のキャンプ監査補完・職員メモは未着手。

新規6ファイル：`supabase/migrations/202609110015_application_payments.sql`、`app/actions/staff-application-operations.js`、`utils/application-operations/validation.js`、`utils/application-operations/queries.js`、`tests/application-operations-actions.test.mjs`、`supabase/tests/application_operations.sql`。変更は既存の地域活動取得、ローカルDBテスト実行スクリプト、この資料・ai_context・database・routesのみ。同時操作4ケースは既存ローカル実行スクリプトへ追加し、専用の新規SQLファイルは作らなかった。

検証結果：対象Node 24テスト成功、DB単一接続91項目成功、別接続4ケース（同じ版で納付更新、納付と審査、待機中の職員停止、REPEATABLE READ）成功。別接続は異なる接続IDとpg_blocking_pidsで実待機を検査し、失敗側の部分更新なしも確認。SQL014→015適用前後の16テーブル完全一致、入居前・滞在中・退去済みの納付更新で滞在・金額等が不変、一時ローカルDBと架空データの後片付けを確認。ローカルAuthは代替実装であり、実Supabase/JWT/画面の受入を代替しない。

再現（Phase 1だけ）：
```bash
node --experimental-vm-modules --test tests/application-operations-actions.test.mjs
node scripts/test-community-applications-db.mjs application_operations.sql --payments-concurrency
```
既存ローカルPostgreSQL runtimeを使用し、.envやSupabase接続先は読まない。実行スクリプトの引数なしは全回帰になるため、Phase 3終了まで実行しない。変更JS6ファイルの対象ESLintも成功（元プロジェクトの既存node_modulesを参照）。初回のSQL権限テストは検証用UPDATEにRETURNINGがなくカーソルエラーになったため、検証SQLを修正して成功。lint初回の依存解決エラーも既存node_modulesのNODE_PATH指定で解消。

Supabase適用、commit、push、全回帰・全体lint・buildは未実施。T13は画面未接続・Supabase未適用のため引き続き作業中。SQL015を本番でまだ実行しない。

### T14 Phase 2：入退去・早期退去（2026年9月11日）

開始時のworktreeがcleanであることを確認し、main `434cc63` 起点に `codex/application-stays` を作成。Phase 1のSQL015はマージ・Supabase適用・実単一接続91項目成功済みとのユーザー確認を前提とした。Phase 2だけを実装し、commit・pushは未実施。

新規ファイルは承認済み `supabase/migrations/202609110016_application_stays.sql` だけ。既存の `app/actions/staff-application-operations.js`、`utils/application-operations/validation.js`、`utils/application-operations/queries.js`、`tests/application-operations-actions.test.mjs`、`supabase/tests/application_operations.sql`、`scripts/test-community-applications-db.mjs`、docsのai_context／database／routes／tasksへ追記した。入退去の状態遷移・解放条件・入力契約はroutes.md末尾に記載。

対象検証：Node34件、DB151項目、別接続9ケース成功。二重入居・二重退去、退去と部屋変更（両順序）、納付と退去、キャンプ二重退去、待機中の別職員停止、REPEATABLE READ、退去待機後の本人新規提出を確認。異なる接続IDとpg_blocking_pidsで実待機を検査し、失敗側の部分更新なしを比較。単一接続では満員から翌日以降の枠解放・本人重複解消・実提出、解放済み15個人枠に重なる新キャンプの部屋割当、キャンプ枠維持、最終日／遅い退去、監査書込失敗時の滞在・部屋・枠・親版の全取消を確認。

SQL015→016前後の16テーブル完全一致と、一時ローカルDB・テストデータの後片付けも確認。初回テストの変数名衝突と片付け対象の誤りは検証コード内で修正し、作成済みIDだけを対象にして再検証した。製品の入退去・解放ロジックは対象検証で成功。ローカルAuthは代替実装であり、実Supabase・JWT・画面の受入を代替しない。

Phase 2だけの再現：
```bash
node --experimental-vm-modules --test --test-name-pattern=stay tests/application-operations-actions.test.mjs
node scripts/test-community-applications-db.mjs --stays-only --stays-concurrency
```
引数なしのDB実行は全回帰なのでPhase 3後まで保留。SQL単体の通常実行はPhase 1＋2、ローカル `--stays-only` はPhase 1のケースをスキップする。今回の対象コマンドでは過去機能の全テストを再実行していない。Supabase操作、全回帰・全体lint/build、画面接続は未実施。Phase 3のキャンプ監査補完・職員メモ、緊急連絡先設定、団体・キャンセル・延長・アカウント初期化は未着手。T14全体は引き続き作業中。

### T15 Phase 3：キャンプ監査・職員メモ（2026年9月11日）

clean確認後、main `f2e4dbb` 起点に `codex/application-audit-notes` を作成。SQL015・016はマージ・Supabase適用・実DB242項目成功済みとのユーザー確認が最新の前提。Phase 3だけを実装し、SQL017のSupabase適用・commit・pushは未実施。

新規ファイルは承認済み `supabase/migrations/202609110017_application_audit_and_staff_notes.sql` だけ。変更した既存ファイル：
- `app/actions/guardian-consent.js`（提出済みキャンプの旧添付保持）
- `app/actions/staff-application-operations.js`（職員メモAction）
- `utils/application-operations/validation.js`、`utils/application-operations/queries.js`（検証・職員取得）
- `tests/application-operations-actions.test.mjs`、`supabase/tests/application_operations.sql`（Phase 3対象ケース）
- `supabase/tests/camp_room_allocations_and_approval.sql`、`supabase/tests/camp_room_allocations_concurrency.sql`（既存の職員監査件数検査をactor_kind=staffへ明示し、追加した利用者監査と区別）
- `scripts/test-community-applications-db.mjs`（SQL017移行一致、メモを含むスナップショット、同時操作）
- docsの `ai_context.md / database.md / routes.md / tasks.md`

キャンプActionは変更不要で、既存入力契約・返却形式を維持。追加の版引数は導入せず、保存・添付のロックと待機後の権限／期限検査を補完。職員メモは申請版で競合を検査し、保存・添付・納付等と同時更新された場合は古い版を拒否する。監査対象とメモ接続契約はdatabase.md・routes.md末尾。

対象検証：Node15件、DB81項目、別接続8ケース成功。メモ追加／編集の競合、納付・キャンプ保存・添付登録との競合、待機中職員停止、REPEATABLE READ、メモ保存待機後の本人保存でメモを保持することを確認。DBでは権限・本文上限・別申請noteId・同値保存・監査失敗時の全取消・利用者監査の主体／前後／非公開性・再送重複防止を検証。Storage補償と提出済み旧添付の保持はNode境界テストで確認。

対象成功後、全回帰を1回実施：**Node203件、DB単一接続1239項目（既存916＋運用323）、別接続81ケース（既存60＋納付4＋入退去9＋監査メモ8）がすべて成功**。全体lint成功（明示的に対象外の.claudeは除外）。buildは既存Google Fonts取得のDNS制限で初回失敗し、コード変更なし・通信許可付きでbuildだけ再実行して成功。Webpackを使用して既存依存パッケージを再利用した。

SQL016→017前後の既存16テーブル完全一致、staff_notesが空であること、過去監査のバックフィルなしを確認。別接続は接続IDとpg_blocking_pidsで実待機を検査し、各専用データ／スキーマ・一時ローカルDBの後片付けまで成功。ローカルAuthは代替実装であり、実Supabase／JWT／Storage／画面の受入を代替しない。

再現：
```bash
# Phase 3だけ
node --experimental-vm-modules --test '--test-name-pattern=notes|audit consent' tests/application-operations-actions.test.mjs
node scripts/test-community-applications-db.mjs --audit-notes-only --audit-notes-concurrency
# 全回帰（今回は実施済み。変更や失敗なしに反復しない）
node --experimental-vm-modules --test tests/*.test.mjs
node scripts/test-community-applications-db.mjs
npm run lint -- --ignore-pattern '.claude/**'
npm run build -- --webpack
```

T15はSQL017未適用・画面未接続のため引き続き作業中。削除・検索・団体メモ・汎用監査基盤、T16以降は追加していない。requirements.md、SQL001〜016、秘密情報、.env.local、docs/apple_event.txt、.claudeは変更していない。

### T16 職員申請検索（2026年9月11日）

main `09125af` 起点の `codex/staff-application-search` でMVP範囲を実装。新規は承認済み `supabase/migrations/202609110018_staff_application_search.sql` だけ。既存の `utils/application-operations/validation.js / queries.js`、`tests/application-operations-actions.test.mjs`、`supabase/tests/application_operations.sql`、DBテストランナー、関連docsを更新した。

active職員限定、固定の引数・返却許可リスト、文字列ワイルドカードの無効化、期間重複、状態値、50件ページング、正規詳細パス、情報非公開、読取専用性を実装。団体、CSV、全文検索拡張、画面、更新Actionは範囲外。対象Node19件とローカルDB42項目が成功。その後の全回帰もNode222件、DB単一接続1281項目、別接続81ケース、lint、本番buildがすべて成功。SQL018は既存データを書き換えず、Supabase適用・実環境受入・commit・pushは未実施。

対象検証：
```bash
node --experimental-vm-modules --test '--test-name-pattern=staff search' tests/application-operations-actions.test.mjs
node scripts/test-community-applications-db.mjs --staff-search-only
```

### T17 地域活動の個人利用の取消（前半、2026年9月11日）

SQL018のmainマージ・Supabase適用・実DB365項目成功後、`codex/community-application-cancellation` で取消だけを実装。新規ファイルは承認済みの `supabase/migrations/202609110019_community_application_cancellation.sql` だけ。継続・延長、団体、キャンプ利用者の自己取消、メール、画面は含めない。

本人は申請済み・審査中・修正依頼・許可（入居前のみ）から理由付きで取消申請中へ変更できる。この時点では日程枠・部屋・料金・滞在を保持する。active職員が理由付きで確定した時だけ取消済みへ変更し、個人日程枠と部屋を全期間解放する。料金・月別内訳・納付・受付番号・入居前滞在・取消理由・履歴は削除しない。古い版、二重操作、本人違い、職員権限喪失、滞在開始後、不整合を拒否し、状態・解放・監査を一括保存する。

対象Node55件、ローカル一時DBのSQL001〜019適用と運用テスト413項目が成功。続く全回帰もNode231件、DB単一接続1329項目、別接続81ケース、lint、本番buildがすべて成功。SQL019のSupabase適用、commit、pushはまだ行っていない。

### T17 地域活動の個人利用の延泊（後半、2026年9月11日）

main `07a21a0` 起点の `codex/community-application-extension` で、承認済みの本番用SQL020を追加。許可済み・入居前または滞在中の通常申請だけを元に、終了翌日から連続する別申請を1件作る。元申請は不変。延泊側は通常の下書き保存・提出・同意書・受付番号・料金・審査・部屋割り・許可・納付・入退去・取消・職員メモを独立して使う。延泊の延泊、他人の申請、重複する有効延泊、通常のD+14〜D+60外、1〜15日外を拒否する。

新規は承認済み `supabase/migrations/202609110020_community_application_extension.sql` のみ。既存Action・検証・取得・メッセージ、運用テスト、関連docsを更新。対象Node150件と運用統合446項目の成功後、全回帰もNode234件、DB単一接続1361項目、別接続81ケース、lint、production buildが成功。SQL001〜020の順次適用と全後片付けも確認した。Supabase適用、実環境テスト、commit、push、画面接続は未実施。本番用SQL020はSupabase適用時に保存する。検証SQL `supabase/tests/application_operations.sql` は保存用クエリではなく、実行時は末尾ROLLBACKのためSQL Editorでは保存しない。

### T18 団体申込の土台（SQL 021・ローカル作成中）

2026年9月11日、mainのSQL020適用・実DB446項目成功後に`codex/community-groups`を作成。団体代表者の下書き作成・保存・申請開始、本人一覧／詳細、団体専有枠、参加者提出期限、共通SG採番、公開／職員カレンダー統合までを実装した。招待・参加者申請、団体審査・部屋・許可・取消、自動期限処理、UI・メールは未着手。

| ファイル | 内容 |
|---|---|
| `supabase/migrations/202609110021_community_groups.sql` | 団体表・履歴・RLS・RPC・枠・採番・カレンダー統合。本番適用時はSQL Editorで保存する |
| `app/actions/community-groups.js` | 代表者の作成・保存・申請開始Server Actions |
| `utils/community-groups/validation.js` | 固定入力対応、構造検査、公開可能なエラーコード |
| `utils/community-groups/queries.js` | 代表者本人の固定項目一覧・詳細取得 |
| `tests/community-groups-actions.test.mjs` | 実モジュールの認可順序・許可リスト・再送・返却検査 |
| `supabase/tests/community_groups.sql` | 架空データ・末尾ROLLBACKの単一接続検証。SQL Editorでは保存しない |

ローカル結果はSQL001〜021順次適用成功、団体DB95項目、別接続4ケース、Node17件が成功。別接続は団体対団体・団体対個人の両順序・同一キー再送について、異なる接続IDと実待機を確認する。全回帰もNode251件、DB単一接続1456項目・別接続85ケース、lint、production buildが成功した。Supabase適用、commit、pushは未実施。`docs/apple_event.txt`と秘密情報には触れない。

### T19 団体招待・参加者下書き（前半、SQL 022・ローカル作成中）

mainへ反映済み・Supabase適用済みのSQL021を前提に、`codex/group-invitations`で前半だけを実装する。代表者のトークン／手入力コード発行・再発行、ログイン後の安全な招待確認、参加期限・予定人数・重複宿泊を検査した参加、団体に紐づく個人下書き、代表者向け最小参加者一覧が対象。参加者個人フォームの保存・提出、団体審査・部屋・許可・取消、期限切れ自動処理、UI・メールは対象外。

新規は`supabase/migrations/202609110022_group_invitations.sql`、`app/actions/group-invitations.js`、`utils/group-invitations/validation.js / queries.js`、`tests/group-invitations-actions.test.mjs`、`supabase/tests/group_invitations.sql`。既存DBテスト実行スクリプトと関連docsだけを更新した。

対象検証はNode 9件、DB単一接続59項目、別接続3ケースが成功。別接続では残り1枠、同一利用者の別UUID同時参加、再発行中の旧招待参加を、異なる接続IDと実待機で確認した。その後の全回帰もNode 260件、DB単一接続1,515項目・別接続88ケース、lint、production buildがすべて成功。本番用SQL022はSupabase適用時に保存する。検証用`group_invitations.sql`は末尾ROLLBACKを維持し、SQL Editorには保存しない。Supabase適用、commit、push、画面接続は未実施。`docs/apple_event.txt`と秘密情報には触れない。

### T19 団体参加者フォーム・提出（後半、SQL 023・ローカル作成中）

参加者本人の個人情報・緊急連絡先・任意メモ・保護者同意要否の保存、同意書登録、確認済み提出を追加する。団体の日程・場所・目的・町内活動は変更できない。各参加者に個別受付番号と料金を作り、予定人数とactive参加者数が一致して全員提出済みになった最後の提出だけが、団体を`under_review`へ進め招待を失効する。

職員審査、参加者削除・交代、部屋、許可・不許可、取消、期限切れ自動処理、UI・メールはT20以降。本番用`supabase/migrations/202609110023_group_participant_submissions.sql`はSupabase適用時に**保存する**。検証用`supabase/tests/group_participant_submissions.sql`は末尾ROLLBACKのためSQL Editorへ**保存しない**。

ローカルではSQL001〜023の順次適用、新規DB16項目、既存を含むDB単一接続1,531項目・別接続88ケース、Node 265件、lint、production buildが成功。Supabase適用、commit、push、画面接続は未実施。`docs/apple_event.txt`と`docs/presentation/`は別作業の未追跡ファイルとして触れない。

その後、SQL023はmainへマージしSupabaseへ保存・適用済み。関数4件の存在確認は`all_ready=true`、保存しないROLLBACK回帰は`16 / true`をユーザーが確認した。

2026年9月12日、Issue #75で参加者側の入口を画面接続した。`/invite`で16文字のコードを受け付け、`/invite/[token]`でログイン後に許可済みの団体情報だけを表示し、明示確認後に既存の参加Actionへ接続する。ログイン前に団体情報を取得せず、ログインを挟んでも検証済みの招待パスを保持する。参加後の個人情報入力・確認・提出・詳細への画面接続は次Issueとして分離する。新しいSQL、環境変数、依存パッケージは追加しない。

### T20 団体審査・部屋割り・許可（前半、SQL 024・ローカル作成中）

目的／町内活動の確認を先に必須化し、active参加者の審査開始・修正依頼・個別許可、団体単位の部屋別人数割当、全員許可・人数一致後の団体許可と入居前滞在作成、団体不許可時の未終了参加者一括不許可を実装した。部屋割りは参加者名を保持せず、部屋定員・施設15人・割当人数一致を全日で再検査する。全更新は施設→団体→参加者／割当の順にロックし、職員権限・版・監査をDBで検査する。

新規は本番用`supabase/migrations/202609110024_group_review_and_approval.sql`（Supabase適用時に**保存する**）、検証用`supabase/tests/group_review_and_approval.sql`（末尾ROLLBACKのため**保存しない**）、`app/actions/staff-groups.js`、`utils/community-groups/review.js / staff-queries.js`、`tests/staff-groups-actions.test.mjs`。ローカルではSQL001〜024適用、対象DB7項目、対象Node5件、対象ESLintが成功。修正後の団体再提出・参加者削除／交代、許可後減員、団体取消・期限切れ、画面・メール、Supabase適用、commit、pushは未実施。

SQL024はその後mainへマージし、Supabaseで必要な表・制約・権限と9関数の存在を確認した。保存しない検証SQLは`7 / true`で成功した。

### T20 団体変更・取消（後半、SQL 025・ローカル作成中）

修正依頼を受けた参加者本人の保存・再提出、職員による参加者不許可、代表者による対象者削除と招待による交代、団体全体の取消申請・職員確定、許可後の参加者減員を追加した。許可後は1人でも団体専有枠を維持し、0人になった場合だけ団体を取消済みにして枠を解放する。減員と部屋別人数の更新は同一RPCで行い、中途半端な状態を残さない。滞在開始後の取消は拒否する。

本番用`supabase/migrations/202609110025_group_changes_and_cancellation.sql`はSupabase適用時に**保存する**。検証用`supabase/tests/group_changes_and_cancellation.sql`は末尾ROLLBACKのため**保存しない**。ローカルではSQL001〜025の順次適用、対象DB12項目、関連Node41件が成功。その後の全回帰もNode 281件、DB単一接続1,550項目・別接続88ケース、lint、production buildがすべて成功。T21の期限切れ自動処理、画面、メール、Supabase適用、commit、pushは未実施。`docs/apple_event.txt`と`docs/presentation/`には触れない。

SQL025はその後mainへマージし、Supabaseへ保存・適用済み。保存しない検証SQLは`12 / true`で成功した。

### T10・T12 地域活動個人の職員画面接続（2026年9月12日）

職員ホームの申請検索に利用区分の選択を追加し、地域活動個人をキャンプ用URLへ誤って送らず、`/staff/community/applications/[applicationId]`へつなぐ。詳細画面には申請者情報、日程・活動内容、保護者同意書、審査、取消確定、部屋割当、料金・納付、入退去、職員メモ、本人公開履歴を表示し、既存Server Actionsへ接続する。キャンプIDを持つ申請が地域活動用URLへ指定された場合は正規のキャンプ申請URLへ送る。

新しいSQL、環境変数、依存パッケージは追加しない。一般利用者で職員URLを開くと職員専用の拒否画面になることをChromeで確認した。Node 334件、lint、production buildが成功した。実職員と架空申請`SG-2026-0132`で詳細を表示し、審査開始、部屋「桐」の割当、「MVP受入確認済み」で許可、納付期限2026年9月30日、職員メモを画面から保存した。最終状態は許可・未納・入居前で、申請済みから審査中、許可への履歴を確認した。PC幅と390px幅は横スクロールなし、ブラウザー警告・エラー0件。修正依頼、不許可、取消確定、入退去、実Storageは未確認。

### T18 団体代表者画面接続（2026年9月12日）

既存のSQL021とServer Actionsを使い、代表者向けの団体一覧、新規、編集、確認、受付完了、詳細を実装した。利用者ホームと新規申請選択にも導線を追加し、初回表示だけでは下書きを作らない。保存中・申請中は二重操作を防ぎ、エラー時は入力を保持する。招待・参加者・職員審査は対象外。

架空利用者で`SG-2026-0133`を作成し、2人、2026年10月10日〜11日、申請中、提出期限2026年9月19日23:59が完了・詳細・一覧で一致することをChromeで確認した。公開カレンダーでも対象2日が利用不可へ変わり、団体専有枠を確認した。PC幅と390px幅の表示、Node 339件、lint、production buildが成功。SQLの追加・保存・適用はない。

### T21 団体期限切れ自動処理（SQL 026・ローカル作成中）

期限を過ぎても条件未達の団体を、未終了参加者・招待・部屋・日程枠・履歴・system監査と同じトランザクションで終了する。全員提出済みなら取消せず審査中へ進める。Supabase Cronは毎分DB関数を直接呼び、Edge Function・公開Route・秘密鍵を追加しない。施設更新前にも期限処理し、公開カレンダーはGETで更新せず期限切れ枠を除外する。

本番用`supabase/migrations/202609110026_group_deadline_expiration.sql`はSupabase適用時に**保存する**。検証用`supabase/tests/group_deadline_expiration.sql`は末尾ROLLBACKのため**保存しない**。対象DB10項目、全DB単一接続1,560項目、Node 281件、lint、本番buildが成功。Cronと提出の別接続では実待機後に提出を維持し、誤取消しなく審査中へ進むことを確認。Supabase適用、commit、push、画面は未実施。

### T20 職員の団体審査・部屋割り・許可・取消画面（2026年9月12日）

Issue #80として、`/staff/community/groups`に団体名・状態検索と50件ページング、`/staff/community/groups/[groupId]`に目的確認、参加者ごとの審査開始・修正依頼・許可・不許可、人数単位の部屋割り、団体許可・不許可、取消確定、許可後減員を接続した。目的確認、最終許可、不許可、取消、減員は画面とServer Actionの両方で明示確認を必須にする。部屋割りは保存前に対象人数との一致を検査し、参加者名を部屋へ保持しない。

ローカルでは対象11件、全Node351件、lint、production buildが成功した。新しいSQL、環境変数、依存パッケージは追加せず、SupabaseへのSQL保存・適用もない。実職員による主要状態遷移、PC幅・スマートフォン幅、ブラウザー警告の受入確認は未実施であり、完了扱いにしない。

### T09 職員カレンダー・利用停止期間画面（2026年9月12日）

Issue #78として、`/staff/calendar`に月移動、日付選択、キャンプ・地域活動個人・団体・利用停止の区別、日別内訳と正規詳細URLを接続した。`/staff/calendar/blocked-periods`配下には月別一覧、新規、編集、削除を接続した。内部理由は職員用取得結果だけから表示し、公開カレンダーへ渡さない。日別のキャンプ合計行と参加者行は別項目として表示し、人数を二重集計しない。

削除は理由と明示確認を画面・Server Action双方で必須にし、エラー時は入力と許可済みの競合概要だけを保持する。ローカルでは対象33件、全Node356件、lint、production build、差分検査が成功した。新しいSQL、環境変数、依存パッケージは追加せず、SupabaseへのSQL保存・適用もない。

実職員でChromeへログインし、2026年10月のキャンプ・地域活動個人・団体の区別、団体の日別内訳と正規詳細リンクを確認した。既存団体と重なる利用停止の作成は安全な競合概要を表示して拒否された。空き日にテスト用利用停止を作成し、別画面で先に更新した後の古い版からの更新拒否、削除確認なしの拒否、明示確認後の削除成功を確認した。テスト用データは削除後の一覧で消失を確認済み。390px幅で横スクロールはなく、ブラウザー警告・エラーは0件だった。

### T04 キャンプ編集・削除・対象者管理画面（2026年9月12日）

Issue #86として、キャンプ詳細から編集・削除画面へ接続し、変更理由、更新日時照合、日程競合、既存申請の影響表示、削除の明示確認を追加した。対象者一覧では有効・無効を区別し、メール変更と無効化に理由・更新日時照合・監査を必須にする。既存申請のメールと一致する対象資格は変更・無効化を拒否する。SQL029はSupabaseへ保存・適用済みで、架空データを最後にROLLBACKする実DBテストは9件すべて成功した。ローカルでは全Node359件、lint、production build、差分検査が成功した。実職員でキャンプ一覧・詳細・編集・削除確認・対象者変更／無効化フォームの表示も確認し、画面確認では実データを変更していない。残る作業はPR作成とレビューである。

### T17 地域活動個人の取消・継続申請画面（2026年9月12日）

Issue #81として、本人詳細から既存SQL019・020の取消と継続申請へ接続する。取消画面はDBの`can_request`を満たす場合だけ理由を送信し、職員確定まで利用枠等を保持することを明示する。継続画面はDBの`can_extend`を満たす通常申請だけを元に、開始日を固定表示し終了日・理由を入力して別の下書きを作る。既存の継続申請がある場合は再作成せず、その詳細へ案内する。一覧と詳細では継続申請を明示し元申請へリンクする。新しいSQL・環境変数・依存関係は追加しない。全Node361件、lint、本番build、差分検査が成功。実利用者で許可済み申請の詳細・取消入力・継続入力を表示確認し、ブラウザーでは状態変更を実行していない。
