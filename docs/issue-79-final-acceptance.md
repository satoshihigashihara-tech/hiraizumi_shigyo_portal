# Issue #79 MVP最終総合確認

実施日: 2026-09-13  
確認対象: `main` の `3ca609fc9ab77f808bac4a4d68511b99e7d392fe` と本番 `https://hiraizumi-shigyo-portal.vercel.app`

## 判定

**合格。重大な未解決不具合は検出されなかった。**

本番データを変更せず、公開画面、認証境界、既存の自動テスト、隔離DB、発表資料、通信障害時の代替資料を確認した。PDF生成と保護者同意書アップロードは下記の既知制限として扱い、利用可能とは説明しない。

## 確認結果

| 対象 | 結果 |
|---|---|
| Nodeテスト | 472件合格、失敗0 |
| ESLint | 合格 |
| Next.js production build | 合格、全ルート生成成功 |
| PostgreSQL 17.6隔離回帰 | 合格 |
| PostgreSQL 18.4隔離回帰 | 合格 |
| 公開トップ、公開カレンダー | 本番200応答、390px幅で横方向のはみ出しなし |
| 匿名の利用者・職員画面 | ログインへ誘導 |
| 一般利用者から職員画面 | `staff-only` として拒否 |
| 本人の申請一覧・部屋表示 | 390px幅の空状態を確認。部屋表示は一度だけ取得失敗後、再アクセスで正常。再現せず |
| PDF配信境界 | 本人用・職員用とも匿名GET/HEADを401で拒否、`private, no-store` |
| 本番DB読取確認 | PostgreSQL 17.6、本人部屋RPCあり、SQL040トリガー1件、PDF active設定0件、部屋mapping有効0件 |
| PDF Storage | bucketあり、非公開 |
| Vercel | Production Ready、対象mainコミットを配備、確認時Error Rate 0% |
| Vercel環境変数 | 公開Supabase URLとanon keyのみ。Production／Previewに設定。secret keyなし |
| 発表スライド | 7ページと発表者ノートを検査。重大な欠落・重なりなし |
| 代替資料 | 6ページを再描画して全ページ目視。職員詳細URLを実装に合わせて修正 |

## 発表時間の確認

デモ以外の短縮原稿をmacOS日本語音声 `Kyoko`、180語/分設定で読み上げた結果は **79.68秒** だった。デモ枠180秒と合わせて **259.68秒（4分19.68秒）** で、5分枠に対して約40秒の余裕がある。

操作待ちが8秒を超えた場合は調査を止め、30秒以内にローカルの `fallback_demo.pdf` へ切り替える。発表当日は話者本人が声量と操作を含めて一度通し、ブラウザーの職員用架空データが用意できない場合は最初から代替資料を使う。

## 既知の制限

- 常設Linux変換実行先が未指定のため、本番の申請PDF生成は停止中。active設定0件を維持する。
- 本番の部屋mappingは有効0件。総合受入だけを理由に有効化しない。
- 保護者同意書アップロードはMVP対象外・本番未対応。`SUPABASE_SECRET_KEY` はVercelへ登録しない。
- 本番の職員用架空申請が準備されていないため、職員の主要画面は自動テスト・隔離DB・認可拒否と代替PDFで確認した。本番データを作って見せる操作は行っていない。
- 本人部屋画面で一度だけ取得エラーを確認したが、再アクセス後は正常な空状態となり再現しなかった。再発時は時刻とアカウントを記録し、取得ログを調査する。
- 町の承認を必要とするB1以降の制度判断は未着手。MVPの完成範囲へ含めず、承認前に仕様を作らない。

## 当日運用

- 操作順、短い発話原稿、30秒以内の代替切替は `docs/presentation/presenter_runbook.md` を正とする。
- 障害の切り分けとVercelの直前正常デプロイへの復旧は `docs/incident-runbook.md` を使う。
- 発表中は保存、提出、審査、納付、入退去など本番データを変える操作をしない。
- 自主制作の試作版であり、平泉町の公式運営サービスとは説明しない。

## 保存・外部操作

- 本番DBは読取専用SQLだけを実行し、保存していない。
- Supabase、Vercel、本番利用者データの変更なし。
- この受入で変更するのは発表資料、代替資料、手順書、本記録だけ。
