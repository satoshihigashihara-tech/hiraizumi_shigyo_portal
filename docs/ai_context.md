# AI・新規参加者向けプロジェクト概要

## 1. このシステムは何か

「ひらいずみ志業ポータル」は、シェアハウスの利用希望者が町へ**使用許可を申請**し、町職員が審査・部屋・料金・納付・入退去を管理する試作版です。ホテルの空室を即時予約するシステムではありません。申請を送っても、その場で利用確定にはなりません。

最初に扱うのはスパルタキャンプ利用です。職員がキャンプ期間と対象メールアドレスを登録し、対象者だけが固定期間の個人申請を提出します。地域活動の個人利用はバックエンドまで対応し、団体利用は今後追加します。

## 2. 利用者と役割

- 未ログイン利用者：公開案内と公開カレンダーを見る。
- キャンプ利用者：自分が対象になっているキャンプへ個人申請する。
- 地域活動の個人利用者：条件内の日程で個人申請する（バックエンド実装済み、画面未作成）。
- 団体代表者・参加者：将来、招待を通じて団体利用を申請する。
- 町職員：申請、対象者、部屋、料金、納付、入退去、履歴を管理する。

本人の申請と職員業務以外から、住所・電話・緊急連絡先・同意書を取得してはいけません。団体代表者にも、参加者個人のこれらの情報を見せません。

## 3. 技術と責任分担

- Next.js App Router／JavaScript
- Supabase Auth、Postgres、RLS、Storage
- Vercelへの公開を予定
- バックエンド担当：DB、RLS、RPC、Server Actions、業務ルール、統合
- フロント担当：公開・利用者画面、フォーム、表示、レスポンシブ対応
- 接続と受入確認：2人で実施

画面からの更新はServer Actionを通し、重要な権限・期限・定員・状態判定はDBのRPCでも再検査します。ブラウザから送られるID、状態、金額、権限を信用しません。

## 4. 現在地

2026年9月11日時点で、Supabase接続、認証Action、基礎テーブル、キャンプ申請、採番、料金、職員のキャンプ作成・対象者追加、保護者同意書の非公開保存、職員のキャンプ審査・部屋割り・許可処理、日程枠・カレンダー、地域活動個人の申請・修正再提出・最小審査処理があります。SQL `001`〜`013` はSupabaseへ適用済みです（009・010・011・012・013の適用成功はユーザー確認に基づく）。非公開バケット `guardian-consents` も作成済みです。

利用者・職員の業務画面は未作成です。コードやSQLが存在しても、ブラウザで正常系・権限違反・期限切れ・同時送信を確認するまでは機能完成と扱いません。詳細は [tasks.md](tasks.md) を参照してください。

mainへ反映済みのキャンプ個人対応には、修正再提出の期限判定を直すSQL `009` と、キャンプ個人申請の部屋割り・許可を追加するSQL `010`、下書き作成入口の期限判定を直すSQL `011`、対応する職員Actionがあります。SQL 010は部屋別・施設15人の日別検査、状態・職員権限・更新日時の検査、職員専用監査履歴、許可時の入居前登録を行います。SQL 011適用後の単一接続の検証は218項目成功しました。複数接続での同時更新も7ケースすべて成功しました（19:47のユーザー提供画像で確認）。既存3つを含む職員の審査・部屋割りActionには `updatedAt` が必須となるため、SQL 010と更新後のActionをそろえて接続してください。地域活動個人の部屋割りは下記SQL 014で追加（ローカル検証済み・Supabase適用・DB検証・後片付け完了）。団体の部屋割り、画面接続は残作業です。

初回の検証では、下書き作成入口がトランザクション開始時刻の `now()` で判定していたため、処理途中の期限超過を拒否するケースで失敗しました。ユーザー確認後にSQL 011を作成・適用し、判定を `clock_timestamp()` へ変更しました。入力・返却値・下書き再利用の仕組みは維持しています。

当時の `supabase/tests/camp_room_allocations_and_approval.sql` はSQL 001〜011で実行しました。現在の検証ファイルはSQL 012の枠を含むため001〜012が必要です。ユーザーが更新後のファイル全体をSQL Editorで再実行し、2026年9月10日15:42の提供画像で `passed_checks = 218` と単一接続である旨の表示を確認しました。架空データと一時ヘルパーを使い、末尾の `ROLLBACK` で検証用変更を取り消す構成です。`npm run lint`・`npm run build` と関数差分の静的確認も成功しています。複数接続での定員競合・同一申請の競合・ロック待ち中の期限超過は、別の同時更新テストで確認済みです。実ログイン・実ファイル添付・画面接続は別途確認が必要です。

`supabase/tests/camp_room_allocations_concurrency.sql` の修正版を再準備し、A・B両方の実行成功後に `verify()` を実行しました。2026年9月10日19:47のユーザー提供画像で、ケース1〜7すべての `passed = true`、異なる接続ID（A: `100880`、B: `100891`）を確認しました。拒否コードはケース1〜4・6が `P0001`、ケース5が `40001`、ケース7が `42501` です。部屋・施設の残り1枠、部屋変更と許可の競合、二重許可、REPEATABLE READ、待機中の修正期限超過、待機中の職員停止を検証済みです。今回分もキャンプ7件・利用者25人・申請23件の削除結果と、`cleanup_completed = true` による専用スキーマの削除をユーザーが確認しました。受付番号カウンターは維持しています。詳しくは `docs/tasks.md` 第4.5節を参照してください。

初回の同時更新検証は、Bの条件分岐が分離レベル設定前にスナップショットを取得してSQLSTATE `25001` で停止し、Aもタイムアウトしました。検証コードをCOMMIT直後の設定へ修正し、実際の分離レベルの記録・検査を追加しました。失敗分はキャンプ7件・利用者25人・申請23件を削除し、専用スキーマの不存在と設定切り替えの単独検証にも成功してから再準備しています。受付番号カウンターは巻き戻していません。初回失敗分・修正版の成功分とも後片付けを確認済みです。

### T09の日程管理バックエンド（main反映済み）

T09の作業ブランチは `codex/calendar-claims`（main反映済み）。キャンプ・利用停止に限ったT09バックエンドの実装、SQL 012適用、再現テスト、検証用データの後片付けが完了した。SQL 001〜011と既存の個人審査・部屋割当Actionは変更していない。

SQL 012は `blocked_periods` と `calendar_claims`（camp・blockedのみ）、既存キャンプの枠移行、公開・職員カレンダー取得、キャンプ編集・削除と利用停止管理を追加する。日程更新・下書き作成・提出は施設ロックを共有する。有効な提出済み申請があるキャンプの期間変更・削除は拒否し、下書きだけなら日程と更新日時を同時更新する。古い更新日時、枠競合、待機後の権限・期限違反を拒否する。

Supabaseでは、SQL Editorの「Success. No rows returned」、公開カレンダーのanon権限での取得、単一接続110＋218項目、別接続13＋7ケースの成功をユーザーが確認した。別接続は各 `verify()` の集約結果 `13 / true`、`7 / true` で確認し、具体的な接続ID・個別行・削除件数は取得していない。**両方の検証で `cleanup_completed = true` をユーザーが確認し、今回の `t09_concurrency_test`・`t12_concurrency_test` と架空データの後片付けは完了。** 受付番号カウンターは再利用防止のため維持する。T09の再実行時に出た `already attempted` は、初回が成功済みだったことを後の検証で確認した。

ローカルPostgreSQL 18.4でも単一接続328項目・別接続20ケース・移行前検査を確認済み。Action・取得処理の27テスト、lint・buildも成功した。ローカルはAuthの必要部分を代替し、SupabaseのSQL検証も実ログイン・JWT署名検証・Storage APIの検証とは区別する。結果と再現方法は [tasks.md 第4.6節](tasks.md#46-t09日程管理バックエンドの検証と引き渡しsql-012) を参照。

取得・Actionの接続契約は [routes.md 第9.4節](routes.md#94-t09バックエンドの接続契約sql-012) に記載。公開応答は日付と利用可否だけ。`createStaffCamp` のエラーはクエリ遷移から `{ error, fields, conflicts }` 返却へ変わる。更新日時はマイクロ秒を保持し、期限入力は指定分の終わりまでを受け付ける境界へ変換する。

T09全体は作業中。SQL 012では個人・団体の申請や部屋割当を実装していない。個人枠は下記T10、団体枠はT18以降。画面接続・実ログインの受入確認は残る。ユーザーへの操作案内は1つずつ、想定結果を先に示し、一致なら短い返答で確認する。全行のスクリーンショットを求めず、検証SQLの集約結果を使う。

### T10の地域活動個人バックエンド（SQL 013・Supabase適用済み）

作業ブランチは `codex/community-individual-applications`。**承認済み範囲のバックエンド実装、SQL 013のSupabase適用、ローカル・Supabaseでの単一接続558項目（T10の230＋カレンダー110＋キャンプ218）と別接続40ケース（T10の20＋キャンプ7＋カレンダー13）の検証、全後片付けが完了。** Supabaseは2026年9月11日のユーザーによる適用成功・各集約結果の確認に基づく。別接続はA実行中にBを開始して両方終了後に `verify()` を実行し、`20 / true`、`7 / true`、`13 / true` を確認。個別行や接続IDの出力は取得していない。3種類とも専用の `cleanup()` とスキーマ削除後に `cleanup_completed = true` をユーザーが確認し、受付番号カウンターは維持した。今回の準備SQL・A・B・後片付けを再実行する必要はない。画面・実ログイン・実Storageの受入確認は未実施のため、T10全体は「作業中」とする。SQL 001〜012と `requirements.md` は変更していない。適用済みのSQL 001〜013は以後書き換えず、DB修正が必要なら新しいマイグレーションを追加する。

検証準備時には申請0件の既存テスト用キャンプ（2026年9月24〜30日）が枠を占有していたため、ユーザーの承認後、施設ロック・更新日時・申請0件・枠一致を再検査する管理SQLで論理削除・枠解放・監査を一括保存した。`camp_deleted = true / claim_released = true / application_count = 0` をユーザー確認済み。対象ID・元の更新日時・ローカル検証の根拠は [tasks.md 第4.7節](tasks.md#47-t10地域活動個人の実装と検証sql-013) に記録している。

SQL 013は `community_individual` の下書き作成・保存・提出・修正再提出・本人取得、個人1人分の枠、受付番号、料金、履歴を追加する。職員操作は審査開始・修正依頼・不許可だけ。キャンセル／延長（T17）、許可／部屋割り（T12）、納付更新（T13）、入退去（T14）、団体（T18以降）は実装していない。

提出時のJST当日をDとして開始≧D+14、終了≦D+60、両端を含む2〜15日を検査する。同日程の修正再提出には初回受付窓を再適用せず、職員が指定した修正期限があれば検査する。個人に団体の「3日後」や期限切れ自動取消を適用しない。変更候補日は `revision_start_date / revision_end_date` に保存し、再提出成功まで元の枠・料金を維持する。

利用者・職員・添付メタデータの更新はSQL 012の施設ロックを共有する。本人／職員のactive状態を待機後も確認し、`updatedAt` をマイクロ秒まで一致させる。新しい提出ごとにUUIDの `submissionKey` を用意し、同じ通信の再送では元のキーと版を再利用する。サービス権限は同意書の保存・登録に限定。地域活動の添付は親申請の版を更新し、提出済みの旧ファイルを削除せず職員監査にパスを残す。

ローカルPostgreSQL 18.4で単一接続558項目（既存218＋カレンダー110＋T10の230）、別接続40ケース（既存7＋13＋T10の20）と全後片付けが成功。Action・取得・認証接続・添付境界の74テスト、lint・buildが成功（buildは既存フォント取得の通信許可後に成功）。ローカルAuthは代替実装であり、SupabaseのSQL検証も実ログイン・JWT署名検証・Storageの実ファイル検証を代替しない。詳細は [tasks.md 第4.7節](tasks.md#47-t10地域活動個人の実装と検証sql-013)、画面契約は [routes.md 第9.5節](routes.md#95-t10バックエンドの接続契約sql-013)。

無関係な未追跡ファイル `docs/apple_event.txt` は変更・削除・コミットしない。秘密鍵と `.env.local` の中身は表示・コミットしない。Supabaseの操作は想定結果を説明してから1つずつ案内する。SQLを案内する際は毎回「既存クエリを置き換える」か「New query」を明記する。また、そのSQLを確認後に「保存する」か「削除してよい」かを毎回明記する。「削除」はSQL Editorのクエリを指し、DBのデータやプロジェクトのSQLファイルの削除とは区別する。新規ファイル作成前にはファイル名と目的を説明する。今回のテスト用クエリは検証・後片付け・記録が完了したため削除してよい。SQL 013適用用と空キャンプ論理削除用の管理クエリは保存し、プロジェクト内のSQLファイルも保持する。

### T12の地域活動個人の部屋割当・許可（SQL 014・Supabase適用・DB検証・後片付け完了）

2026年9月11日、承認済みの既存10ファイル・新規4ファイルの範囲で `codex/community-individual-room-approval` に実装した。新規マイグレーションは `202609110014_community_individual_room_allocations_and_approval.sql`。**SQL 001〜013は変更せず、014はローカル検証済み・Supabase適用・DB検証・後片付け完了。** `requirements.md` と `docs/apple_event.txt` も変更していない。

`assignCommunityApplicationRoom` と `approveCommunityApplication` を既存職員Actionへ追加。初回割当は審査中、変更は審査中または許可後（入居前・滞在中）のみ。人数1・提出済み期間全体をDBで固定し、施設枠を二重計上しない。部屋・施設15人の日別定員、キャンプ・停止・本人重複、枠と期間の一致を保存／許可時に再検査する。許可時は状態・入居前滞在・公開履歴・職員監査を一括保存する。既存割当変更・日程変更後の再割当は理由必須。最新版の同内容保存は無更新、古い版は拒否する。

SQL 013の日程変更再提出による旧部屋の全解放を維持し、再審査後に理由付きで同じ割当行を新期間へ再割当できる。同日程の修正は部屋を維持する。内部の部屋変更理由は本人に返さない。本人取得へ `approval_comment / room_allocation / stay`、職員へ読取専用の部屋確認データを追加。`room_allocation.is_current=false` の旧割当を現在の部屋として表示しない。

ローカル・Supabaseとも単一接続916項目（新規358＋既存地域活動個人申請230＋カレンダー110＋キャンプ218）、別接続60ケース（新規20＋既存地域活動個人申請20＋キャンプ7＋カレンダー13）、4種類の専用データ／スキーマの後片付けが完了。SupabaseのSQL 014適用、各集約結果と4つの `cleanup_completed = true` はユーザー確認に基づく。各別接続はA実行中にBを開始し、両方終了後のverifyで別接続・実待機・失敗側の部分更新なしを検査した。個別行・接続IDの出力は取得していない。受付番号カウンターは維持しており、今回の準備SQL・A・B・後片付けの再実行は不要。

ローカルでは013の実RPCで作ったキャンプ許可・地域活動修正中のデータが014適用前後の16テーブルで完全一致し、一時DBの停止・削除も成功。Node 112テスト（既存74＋新規38）、lint・buildが成功。バックエンドは引き渡し可能だが、画面・実ログイン・実Storageの受入確認は未実施で、T12全体は「作業中」とする。変更は未コミットで、mainへの反映も未実施。適用済みSQL 001〜014は以後書き換えず、DB修正は新しいマイグレーションで行う。詳細は [tasks.md 第4.8節](tasks.md#48-t12地域活動個人の部屋割当許可sql-014)、接続契約は [routes.md 第9.6節](routes.md#96-t12地域活動個人の部屋割当許可sql-014)。

SQL Editorのテスト用クエリは検証・後片付け・記録が完了したため削除してよい。SQL 014適用用クエリは保存し、プロジェクト内のSQLファイルも保持する。

対象は個人の部屋割当・許可だけ。キャンセル・延長・納付更新・入退去・早期退去・団体は追加していない。新RPCは延長元付き申請も対象外とし、キャンプの既存Action／RPCは維持する。これらの状態を扱うテスト用の直接更新は製品の操作ではない。部屋変更で許可期間・料金・納付・滞在状態を変更しない。

## 5. 必ず守る業務ルール

- 施設全体は15人。部屋別定員も超えない。
- キャンプ利用期間は職員が設定した固定期間。
- キャンプの対象メールと一致する本人だけが申請できる。
- 受付番号、料金、枠確保は提出成功時に行い、下書き時には行わない。
- 料金は1日300円、1暦月9,000円上限。
- 未成年者と18歳の高校生は、申請ごとに保護者同意書が必要。
- 同意書は非公開Storageに置き、公開URLや氏名・メールをパスへ含めない。
- 「申請状態」「納付状態」「滞在状態」を混同しない。
- 閲覧しただけで審査中などへ状態変更しない。
- 日時判定と表示は日本時間を基準にする。

## 6. 判断の優先順位

1. [requirements.md](requirements.md) — 確定した要件
2. [routes.md](routes.md) — URLと画面遷移
3. [database.md](database.md) — DB、権限、同時更新の設計
4. [tasks.md](tasks.md) — 現在の進捗と次の作業
5. [coding_rules.md](coding_rules.md) — 実装時の共同ルール

資料同士や実装と資料が食い違う場合は、黙って推測せず差分を記録し、業務内容を変える判断は確認を取ります。

### T13 Phase 1 追記（2026年9月11日）

ユーザー承認のPhase 1（納付）だけをローカル実装した。SQL015は未適用。キャンプ／地域活動個人共通の納付期限・未納／納付済み更新、差戻し理由、監査、取得時JST期限超過フラグを追加。既存Actionの入力契約と既存料金額・申請状態・滞在状態は維持。対象Node24テスト、DB91項目、別接続4ケース、014→015の既存16テーブル一致、対象JSのESLintが成功。詳細・再現コマンドはtasks.md末尾、接続契約はroutes.md末尾。

Phase 2・3は未着手。Supabase適用・commit・pushは禁止の指示に従い未実施。全回帰と全体lint/buildはPhase 3後まで保留。新規は事前説明済みのうちPhase 1用6ファイルに限定し、同時操作テストは既存ローカル実行スクリプトを使用。requirements.md、SQL001〜014、秘密情報、docs/apple_event.txtは変更しない。次はPhase 1差分のレビューであり、承認なしでPhase 2へ進めない。

### T14 Phase 2 追記（2026年9月11日）

最新の前提：SQL015のPhase 1はmain `434cc63` にマージ済み、Supabase適用と実単一接続91項目成功をユーザー確認済み。以前の「SQL015未適用・未マージ」記述は作業当時の記録。

clean確認後、上記main起点に `codex/application-stays` を作成し、承認されたPhase 2だけを実装した。新規はSQL016だけ。許可済みの入居前→滞在中→退去済み、DB時刻、翌日解放（予定終了日の翌日を上限）、個人枠／部屋解放とキャンプ全体枠の維持、必要な入退去監査・取得・Actionを追加。料金・申請期間・申請状態・受付番号は維持し、既存納付Actionの契約を変えていない。

ローカル対象Node34件・DB151項目・別接続9ケースと後片付け、SQL015→016の既存16テーブル一致を確認。詳細・再現はtasks.md末尾、接続契約はroutes.md末尾。Supabase操作・commit・push、全回帰・全体lint/buildは未実施。Phase 3以降や画面には着手していない。SQL001〜015・requirements.md・秘密情報・.env.local・docs/apple_event.txt・.claudeは触らない。SQL016は未適用なので、承認なしにSupabase操作やPhase 3へ進めない。

### T15 Phase 3 追記（2026年9月11日）

最新前提はmain `f2e4dbb`、SQL015・016のSupabase適用と実application_operations.sql 242項目成功（ユーザー確認）。clean確認後 `codex/application-audit-notes` にPhase 3だけを実装。新規はSQL017だけで、Supabase操作・commit・pushは未実施。

キャンプの下書き作成／保存・提出／再提出・同意書登録／差替を、既存RPC契約のまま同一トランザクションで監査する。PII実値はコピーせず、状態等・変更項目名と同意書の非公開参照を記録。職員操作監査と重複させない。active職員だけの個別申請メモ追加・編集・取得を追加し、本文1〜2000文字、親申請のupdated_at競合検査、監査との一括保存を実装。本人応答へメモを渡さない。既存キャンプActionの版引数は増やさず、ロックと待機後の権限／期限検査を補完。同意書失敗補償を維持し、提出済み旧添付を保持する。

対象Node15・DB81・同時操作8ケース成功後、全回帰Node203・DB1239・同時操作81ケース、lint・buildが成功。buildだけ既存フォント取得のネットワーク制限から通信許可付きで再実行した。移行前後16テーブル一致・新メモ表空・全後片付けも確認。実Supabase／Storage／画面は未検証で、SQL017未適用。詳細・ファイル一覧・再現はtasks.md末尾、契約はroutes.md末尾。T16以降や範囲外機能へ進めない。

### T16 職員申請検索追記（2026年9月11日）

SQL017のmainマージ・Supabase適用・実DB323項目成功をユーザー確認後、main `09125af` から `codex/staff-application-search` を作成。新規は承認済みSQL018だけ。既存2区分のactive職員専用検索RPCとServer取得を実装し、団体・画面・CSV・更新処理は追加していない。対象Node19件・DB42項目成功後、全回帰Node222件・DB単一接続1281項目・別接続81ケース、lint・build成功。build初回だけ既存Google Fontsの通信制限で失敗し、通信許可後にコード変更なしで成功。SQL018のSupabase適用、commit、pushは未実施。接続契約はroutes.md末尾、DB境界・非公開項目はdatabase.md末尾、再現はtasks.md末尾を参照する。

### T17 個人取消追記（2026年9月11日）

最新前提はmain `185676f`、SQL018はマージ・Supabase適用・実DB365項目成功済み。SQL019で「地域活動の個人利用」の本人取消申請と職員確定だけを追加する。申請中は枠・部屋を保持し、職員確定時だけ全期間解放する。許可後は入居前だけ申請可能で、滞在開始後は町への連絡と既存の早期退去処理を使う。料金・滞在・履歴は削除しない。継続・延長、団体、キャンプ自己取消、画面は未実装。

### T17 個人延泊追記（2026年9月11日）

最新前提はmain `07a21a0`。SQL019はmain・Supabaseへ適用済みで実DB413項目成功。`codex/community-application-extension` のSQL020で地域活動個人の延泊バックエンドを実装中。許可済みの通常申請から、元終了日の翌日開始となる別申請を1件だけ作る。元申請を延ばさず、延泊側に別の枠・受付番号・料金・審査・部屋・滞在・取消・職員メモを持たせる。延泊の延泊と重複延泊は不可。団体・メール・画面は含めない。

ローカル対象Node150件・運用統合446項目が成功後、全回帰Node234件、DB単一接続1361項目、別接続81ケース、lint、production build、全後片付けまで成功。SQL020のSupabase適用・実環境検証・commit・push・画面接続は未実施。本番用SQL020は適用時に保存、検証用application_operations.sqlはSQL Editorに保存しない。`docs/apple_event.txt` と秘密情報には触れない。

### T18 団体申込の土台追記（2026年9月11日）

最新前提はSQL020がmain・Supabaseへ適用済みで、実DB446項目成功。`codex/community-groups`では団体代表者による共通情報の下書き作成・保存・申請開始、本人一覧／詳細取得だけを実装する。申請開始時に2〜15人・2〜15日・D+14〜D+60を検査し、施設全体の排他的なgroup枠、参加者提出期限、個別申請と共通のSG受付番号を一括確定する。公開・職員カレンダーと既存の個人／キャンプ／利用停止の競合検査もgroup枠へ対応する。

招待・参加者申請（T19）、団体審査／部屋割り／許可／取消（T20）、期限切れ自動処理（T21）、画面・メールは含めない。本番用SQL021はSupabase適用時に保存する。検証用`community_groups.sql`は保存せず、末尾ROLLBACKを維持する。ローカルではSQL001〜021適用、団体単一接続95項目、別接続4ケース、Action／取得17テストが成功。全回帰もNode251件、DB単一接続1456項目・別接続85ケース、lint、production buildが成功した。Supabase適用、commit、pushはまだ実施していない。
